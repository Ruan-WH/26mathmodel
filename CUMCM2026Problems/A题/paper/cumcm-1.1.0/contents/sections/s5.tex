\section{模型准备}

已有研究对吸湿性毛细多孔材料的对流干燥进行了数值模拟\cite{dietl1998}，并将温湿场计算扩展至热风、红外与微波联合干燥\cite{salagnac2004}。本文以题给热风边界和物性关系为依据建立模型，不引入辐射或微波热源。

\subsection{几何降维与初始条件}

药材的初始几何及坐标约定见\cref{fig:geometry}。取一端面中心为原点 $O$，$z$ 轴沿药材轴线，$x$、$y$ 轴位于端面内；圆柱长 $L=25$~cm，初始半径 $R_0=2$~cm。中截面中心记为 $O_m$，径向距离 $r$ 从该点起算，周向角 $\theta$ 从正 $x$ 轴起算。图中热量传入箭头示意预热阶段，水分箭头表示向外迁移。

在圆柱坐标 $(r,\theta,z)$ 中，轴对称条件使 $\partial/\partial\theta=0$。主模型进一步取中截面 $z=L/2$，忽略该处的轴向梯度，计算域为 $0\le r\le R(t)$。固定半径问题中 $R=R_0=0.02$~m；移动边界问题稍后映射到 $0\le\xi\le1$。初始条件为
\begin{equation}
T(r,0)=28~{}^\circ\mathrm C,\qquad C(r,0)=2.55~\mathrm{kg/kg}.
\label{eq:initial}
\end{equation}

初始中截面至端面的距离 $L/2$ 为半径的 6.25 倍，较长的轴向传递路径为中部径向近似提供几何依据。后文保持相同物性和表面交换条件，以含端面传递的二维模型检查达标时间及控制位置；该对照检验降维误差，均匀、各向同性仍是依据题给标量物性采用的等效化假设。

\begin{figure}[H]
\centering
\includegraphics[width=0.84\textwidth]{geometry_coordinates.pdf}
\caption{药材初始几何、坐标系与径向计算域（中截面单独放大）}
\label{fig:geometry}
\end{figure}

\subsection{附件数据检查与边界重构}

附件数据的结构与采用方式汇总于\cref{tab:data}。附件一、二分别含 241 个和 145 个等间隔采样时刻，采样间隔分别为 60~s 和 1800~s，均不同于 1~s 的数值积分步长，因此需通过插值获得各积分时刻的边界值。

\begin{table}[!htbp]
\centering
\caption{附件时序数据及模型处理方式}
\label{tab:data}
\begin{tabular}{lccl}
\toprule
数据 & 覆盖区间 & 采样间隔 & 模型处理\\
\midrule
环境温度 $T_e$ & 0--14400 s & 60 s & PCHIP，区间外取 $50~{}^\circ$C\\
环境水分 $C_e$ & 0--14400 s & 60 s & PCHIP，区间外取 0.05\\
药材半径 $R$ & 0--259200 s & 1800 s & 单调 PCHIP 并同步求导\\
\bottomrule
\end{tabular}
\end{table}

采用分段三次 Hermite 保形插值（PCHIP），节点斜率按相邻割线斜率确定，使单调数据区间不产生新的极值。该方法避免半径在快速收缩段出现非物理回弹，并保持一阶连续，便于稳定求取问题四所需的 $\dot R(t)$。

附件一存在小幅波动，故环境量保留实测序列的 PCHIP 重构，不强制单调化。最后 1~h 的温度和环境水分均值分别为 $49.9989~{}^\circ\mathrm C$ 和 0.049988~kg/kg，据此假设 14400~s 后工况保持为 $50.000~{}^\circ\mathrm C$ 与 $0.05000~\mathrm{kg/kg}$，避免无限沿用末个采样点的波动。末段稳定性支持这一情景的选取，但不能确定后续数十小时的实际边界；后文第\ref{sec:boundarysensitivity}节进一步量化环境偏离设定值对达标时间的影响。

\subsection{径向热湿控制方程}

基于 Fourier 导热定律和 Fick 扩散定律\cite{luikov1975}，固定半径域内的控制方程为
\begin{align}
\rho(C)c_p(C)\frac{\partial T}{\partial t}
&=\frac{1}{r}\frac{\partial}{\partial r}\left[rk(C)\frac{\partial T}{\partial r}\right],\label{eq:heat}\\
\frac{\partial C}{\partial t}
&=\frac{1}{r}\frac{\partial}{\partial r}\left[rD(T,C)\frac{\partial C}{\partial r}\right].\label{eq:mass}
\end{align}
式中 $\rho c_p$ 是单位体积热容量，决定温度对净热通量的响应速度；$D$ 是以题设浓度变量定义的等效扩散系数。问题一中除 $D(C)$ 外各物性为常数，问题二至四中的密度、比热容、导热系数和水分扩散系数均随状态更新。

轴线处没有净通量，故满足
\begin{equation}
\left.\frac{\partial T}{\partial r}\right|_{r=0}=0,\qquad
\left.\frac{\partial C}{\partial r}\right|_{r=0}=0.
\label{eq:center}
\end{equation}
侧表面采用“向外法向通量等于表面对流通量”的约定：
\begin{align}
-k\left.\frac{\partial T}{\partial r}\right|_{r=R}
&=h_T[T(R,t)-T_e(t)],\label{eq:heatbc}\\
-D\left.\frac{\partial C}{\partial r}\right|_{r=R}
&=h_m[C(R,t)-C_e(t)].\label{eq:massbc}
\end{align}
当 $T_e>T(R,t)$ 时，\cref{eq:heatbc}给出向内热流；当 $C(R,t)>C_e$ 时，\cref{eq:massbc}给出向外水分通量。附录2给出了问题一的交换系数，后续附录仅更新材料物性。本文假设同一烘房内后续过程仍可沿用这组系数；问题四显式更新面积和几何尺度，系数本身的流动及几何依赖不另行标定。

$C$ 为药材水质量与干物质质量之比。附件环境水分量虽也以 kg/kg 表示，但题面未明确其质量基准及与材料平衡含水率的转换关系。为闭合\cref{eq:massbc}，本文假设附件数值可直接用作材料侧的等效环境边界值 $C_e$，$h_m$ 则作为该变量下的有效交换系数。若原始环境量以干空气质量为基准，须结合吸湿平衡关系重新换算；现有预测以这一等效映射成立为条件，环境水分扰动检验不能替代该映射的标定。

\subsection{物性关系及耦合路径}

问题一采用题面附录2给出的热物性常数与水分扩散系数公式
\begin{equation}
\rho=820,\quad c_p=2600,\quad k=0.36,\quad
D=7.0\times10^{-9}\exp(-0.89/C).
\label{eq:prop1}
\end{equation}
问题二、三采用题面附录3给出的物性关系
\begin{equation}
\begin{aligned}
\rho(C)&=650+128C,\\
c_p(C)&=1450+2736\frac{C}{C+1},\\
k(C)&=0.21+0.38\frac{C}{C+1},\\
D(T,C)&=2.4\times10^{-3}\exp\left(-\frac{0.45}{C}\right)
\exp\left[-\frac{3850}{T+273.15}\right].
\end{aligned}
\label{eq:prop23}
\end{equation}
问题四采用题面附录4给出的物性关系
\begin{equation}
\begin{aligned}
\rho(C)&=760+90C,\\
c_p(C)&=1850+2150\frac{C}{C+1},\\
k(C)&=0.12+0.20\frac{C}{C+1},\\
D(T,C)&=4.2\times10^{-4}\exp\left(-\frac{0.30}{C}\right)
\exp\left[-\frac{3850}{T+273.15}\right].
\end{aligned}
\label{eq:prop4}
\end{equation}

由\cref{eq:prop23,eq:prop4}可知，水分影响储热与导热，温度和水分共同影响 $D$，由此形成状态相关物性耦合。采用题给函数可在统一口径下比较四问的温湿迁移，但这一耦合不包含蒸发耗热；Purlis 的食品对流干燥多相模型则考虑了吸附热，并讨论蒸发速率项及方程闭合问题\cite{purlis2019}。本文未建立液汽相分配及局部汽化速率关系，也未完成潜热与输入热量的量级对照，故不将缺少相变项解释为其影响已足够小；后文收敛与交叉求解仅检验既定方程的数值实现。

\subsection{节点中心有限体积离散}

空间节点取 $r_i=R[1-(1-i/N)^2]$，$i=0,1,\ldots,N$，基准取 $N=160$，对低扩散率表层加密；输出位置仍为每隔 0.1~cm，由计算解插值得到。内部界面位于 $r_{i+1/2}=(r_i+r_{i+1})/2$。按单位轴向长度积分后，第 $i$ 个环形控制体体积和界面面积分别为
\begin{equation}
V_i=\pi(r_{i+1/2}^2-r_{i-1/2}^2),\qquad A_{i+1/2}=2\pi r_{i+1/2}.
\label{eq:cv}
\end{equation}
中心控制体下边界直接取 0，表面控制体上边界取 $R$。这种几何构造自动消除了连续方程在 $r=0$ 处形式上的 $1/r$ 奇点。

以 $\phi$ 统一表示 $T$ 或 $C$，储存系数记为 $s$，扩散系数记为 $\Gamma$。在时间层 $n+1$ 采用全隐式格式，内部节点守恒式为
\begin{equation}
\frac{s_iV_i}{\Delta t}(\phi_i^{n+1}-\phi_i^n)
=G_{i+1/2}(\phi_{i+1}^{n+1}-\phi_i^{n+1})
-G_{i-1/2}(\phi_i^{n+1}-\phi_{i-1}^{n+1}),
\label{eq:fvm}
\end{equation}
其中
\begin{equation}
G_{i+1/2}=\frac{A_{i+1/2}\Gamma_{i+1/2}}{r_{i+1}-r_i},\qquad
\Gamma_{i+1/2}=\frac{\Gamma_i+\Gamma_{i+1}}{2}.
\label{eq:conductance}
\end{equation}
温度场取 $s=\rho c_p,\Gamma=k$，水分场取 $s=1,\Gamma=D$。将\cref{eq:fvm}整理为
\begin{equation}
-a_W\phi_{i-1}^{n+1}+a_P\phi_i^{n+1}-a_E\phi_{i+1}^{n+1}=b_i,
\label{eq:tridiag}
\end{equation}
其中
\[
a_W=G_{i-1/2},\quad a_E=G_{i+1/2},\quad
a_P=\frac{s_iV_i}{\Delta t}+a_W+a_E,\quad
b_i=\frac{s_iV_i}{\Delta t}\phi_i^n.
\]
矩阵主对角元为正，非对角元非正并满足对角占优，既保留局部守恒，也有利于保持温度、水分的离散最大值性质。

中心节点令 $a_W=0$，自然实现零通量条件。表面节点将 Robin 通量直接并入控制体：
\begin{equation}
a_P\leftarrow a_P+A_Rh,\qquad
b_N\leftarrow b_N+A_Rh\phi_e,\qquad A_R=2\pi R,
\label{eq:robindiscrete}
\end{equation}
其中热场取 $(h,\phi_e)=(h_T,T_e)$，水分场取 $(h_m,C_e)$。最终每一场只需求解一个三对角线性系统，计算量随径向节点数线性增长。

\subsection{非线性 Picard 迭代与输出控制}

在每个新时间层，令初始迭代值等于上一时间层结果，并执行：
\begin{enumerate}
  \item 由当前 $C^{(m)}$ 计算 $\rho,c_p,k$，组装并求解温度三对角系统，得到 $T^{(m+1)}$；
  \item 由 $T^{(m+1)},C^{(m)}$ 计算 $D$，组装并求解水分三对角系统，得到 $C^{(m+1)}$；
  \item 检查
  \[
  \|T^{(m+1)}-T^{(m)}\|_\infty<10^{-8},\qquad
  \|C^{(m+1)}-C^{(m)}\|_\infty<10^{-10};
  \]
  若未同时满足则返回步骤 1，最多允许 30 次迭代。
\end{enumerate}

四问终算均采用 $\Delta t=1$~s；问题二、三共享从初态至达标的同一条轨迹，问题三按 60~s 抽取，不再独立积分。程序在每一步检查有限值、$0\le C\le2.55$、径向单调性、Picard 收敛及方程平衡残差；阈值跨越时另存插值得到的首达剖面。空间网格、时间步长与输出采样分别检验，四位小数仅表示结果展示精度。
