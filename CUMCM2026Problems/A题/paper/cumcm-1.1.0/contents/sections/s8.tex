\section{问题三：全截面达标时间模型的建立与求解}

\subsection{全截面达标时间模型}

\subsubsection{全域首达判据}

问题三要求“所有位置”的水分浓度均不超过 $0.15~\mathrm{kg/kg}$，因此不能用截面平均值或表面值代替。定义空间最大水分浓度
\begin{equation}
M(t)=\max_{0\le r\le R_0}C(r,t),
\label{eq:maxmoisture}
\end{equation}
则烘干完成时刻是集合约束的首次满足时间
\begin{equation}
t_d=\inf\{t\ge0:M(t)\le0.15\}.
\label{eq:firstpass}
\end{equation}
该定义包含两个必要条件：一是 $t_d$ 时全空间均达标，二是在任何 $t<t_d$ 时至少存在一个未达标位置。因而它比“取第一个离散输出时刻”更严格，也能避免输出间隔改变时答案随之改变。

初始水分场均匀，表面环境水分始终显著低于药材内部，且有限体积矩阵是对角占优的 M 型结构。在这种向外失水的条件下，数值解应保持由中心向表面非增。程序对每一个内部时间层检查
\begin{equation}
C_i^n-C_{i+1}^n\ge-2\times10^{-9},\qquad i=0,1,\ldots,N-1,
\label{eq:monotonecheck}
\end{equation}
全部时间层均通过。因此
\begin{equation}
M(t)=C(0,t),
\label{eq:centercontrol}
\end{equation}
即轴线是本问的控制位置。这里并非预先假定中心最大，而是先保留全域最大值判据，再由计算结果确认其可化为中心事件。

\subsection{模型的求解}

\subsubsection{长期积分与首达区间搜索}

沿用问题二的网格、1~s 步长及附件三物性，从初始状态连续推进，不在 3~h 处重新初始化。附件一时间范围内采用 PCHIP 环境值，超过 4~h 后取 $T_e=50~{}^\circ\mathrm C$、$C_e=0.05~\mathrm{kg/kg}$。每步先完成温湿场 Picard 迭代，再计算全部径向节点中的最大水分 $M^{n+1}$。



\textbf{Step 1 初始化判据：}令 $t_0=0$、$M^0=2.55$，标记尚未达到阈值。

\textbf{Step 2 推进状态：}按问题二交替求解温度和水分，并检查\cref{eq:monotonecheck}。

\textbf{Step 3 寻找跨越：}若 $M^{n+1}>0.15$，令 $n\leftarrow n+1$ 继续积分；第一次出现 $M^n>0.15$ 且 $M^{n+1}\le0.15$ 时，确定首达区间 $[t_n,t_{n+1}]$。


搜索使用全域最大值，中心控制仅在径向单调性检验通过后得到确认。如此可同时满足“全域均达标”与“首次达标”的要求。


\subsubsection{阈值事件定位方法}

长期积分使用 $\Delta t=1$~s。设第 $n$ 层尚未达标而第 $n+1$ 层已经达标，即
\[
M^n>0.15,\qquad M^{n+1}\le0.15.
\]
在这两个相邻时间层间定义
\begin{equation}
\lambda=\frac{0.15-M^n}{M^{n+1}-M^n},\qquad
t_d=t_n+\lambda\Delta t,
\label{eq:eventinterp}
\end{equation}
并同步插值得到达标剖面
\begin{equation}
C(r,t_d)=C^n(r)+\lambda[C^{n+1}(r)-C^n(r)].
\label{eq:profileinterp}
\end{equation}
由于 $0\le\lambda\le1$，所得时刻位于数值解跨越阈值的 1~s 区间内。时间精度由积分步长和事件插值决定，题目要求的 60~s 取值间隔只用于结果展示。




\subsubsection{达标剖面与数值精度核验}

确定 $t_d$ 后，使用同一插值权重计算温度、水分剖面，核对最大水分等于阈值且其余位置不超过阈值。每 60~s 的径向分布由同一连续轨迹取值；若首达时间不是整分钟，则继续推进到其后的首个整分钟，以覆盖题目要求的时间序列。

保持物性和边界不变，将时间步从 2~s 减为 1~s，达标时间由 57.4728~h 变为 57.4719~h，相对差为 $1.52\times10^{-5}$。在统一 10~s 步长下，161 与 321 个加密节点的对照相对差为 $1.65\times10^{-5}$。据此采用 161 节点、1~s 步长作为终算配置。

\subsection{结果的分析和验证}

\subsubsection{干燥时间与径向分布}

由\cref{eq:eventinterp}得到
\begin{equation}
t_d=206898.8710~\mathrm{s}=57.4719~\mathrm{h}.
\label{eq:q3time}
\end{equation}
典型时刻及达标瞬间的水分浓度见\cref{tab:q3}。中心从 6~h 的 1.0156 降至 12~h 的 0.4548，随后下降速度逐渐减慢；达标时中心恰为 0.1500，而表面已降至 $0.0525~\mathrm{kg/kg}$，接近稳定环境水分浓度。

\begin{table}[!htbp]
\centering
\caption{问题三每 6~h 及达标时的水分浓度（kg/kg）}\label{tab:q3}
\begin{tabular}{cccccc}
\toprule
$t$/h&0 cm&0.5 cm&1.0 cm&1.5 cm&2.0 cm\\
\midrule
6&1.0170&0.9881&0.9015&0.7550&0.5328\\
12&0.4562&0.4432&0.4031&0.3297&0.1642\\
18&0.2988&0.2916&0.2687&0.2253&0.0845\\
24&0.2378&0.2327&0.2167&0.1855&0.0665\\
30&0.2058&0.2018&0.1892&0.1641&0.0598\\
36&0.1859&0.1825&0.1718&0.1504&0.0566\\
42&0.1721&0.1691&0.1597&0.1407&0.0549\\
48&0.1618&0.1592&0.1507&0.1334&0.0537\\
54&0.1539&0.1514&0.1436&0.1277&0.0530\\
57.4719&0.1500&0.1477&0.1402&0.1249&0.0527\\
\bottomrule
\end{tabular}
\end{table}

中心、表面轨迹及径向剖面见\cref{fig:q3}。表面于 12.5525~h 达标，比中心提前 44.9194~h，应以内部最大水分决定停机时刻。

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.92\textwidth]{q3_threshold_diagnostics.pdf}
\caption{问题三阈值首达过程与径向分布}\label{fig:q3}
\end{figure}

低含水状态下扩散系数减小，后期干燥明显变慢：48~h 中心水分为 0.1618，仍需约 9.47~h 才能达到阈值。

\subsubsection{达标区域由表面向中心的推进}

\cref{fig:q3front}显示已达标区域由表面向中心推进，未达标核心最终于 57.472~h 消失。红线为浓度阈值等值线，不是相变界面。

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.96\textwidth]{q3_drying_front.pdf}
\caption{问题三已达标外层与未达标核心的演化}\label{fig:q3front}
\end{figure}

\subsubsection{本问结论与分析}

时间步与空间网格加密所得达标时间相对差均小于 $2\times10^{-5}$，全部剖面通过径向单调性检查，支持上述首达时间的数值稳定性。
