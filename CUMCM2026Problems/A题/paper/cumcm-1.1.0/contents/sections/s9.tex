\section{问题四：收缩移动边界模型的建立与求解}

\subsection{收缩移动边界模型}

\subsubsection{半径数据重构与固定域映射}

附件二给出的半径由初始 2~cm 持续减小至 1.2~cm。经单调 PCHIP 重构后，在本问主要时刻的半径见\cref{tab:q4radius}。前 6~h 半径已经缩小到 1.374~cm，完成总收缩量的 78.3\%；此后收缩速度逐渐减小，36~h 后在四位小数精度下稳定为 1.200~cm。由此可见，几何变化主要集中在干燥早期，但它对随后全部扩散过程都会持续产生影响。

\begin{table}[!htbp]
\centering
\caption{问题四典型时刻的重构半径}\label{tab:q4radius}
\begin{tabular}{cccccccccc}
\toprule
$t$/h&0&6&12&18&24&30&36&48&51.088\\
\midrule
$R$/cm&2.000&1.374&1.248&1.214&1.204&1.201&1.200&1.200&1.200\\
\bottomrule
\end{tabular}
\end{table}

若直接在物理域 $0\le r\le R(t)$ 上使用固定的 0.1~cm 网格，节点数会随时间减少，旧节点场量还需频繁插值到新网格，不利于保持守恒。为此定义无量纲固定域坐标
\begin{equation}
\xi=\frac{r}{R(t)},\qquad 0\le\xi\le1,
\label{eq:xi}
\end{equation}
并令 $\psi(\xi,t)=\phi(r,t)$。半径收缩时，固定 $\xi$ 的节点随材料截面同步向内移动，始终保留 161 个表面加密计算节点，不需要改变矩阵维数。

\subsubsection{收缩模型方程}

本问采用均匀径向收缩假设，材料速度与材料坐标网格速度相同。令
\begin{equation}
\xi=\frac{r}{R(t)},\qquad 0\leq\xi\leq1,
\label{eq:chainmoving}
\end{equation}
则相对输运速度为零，温度和水分方程均可写成材料坐标下的扩散方程
\begin{equation}
\frac{\partial\psi}{\partial t}
=\frac{1}{R^2\xi}\frac{\partial}{\partial\xi}
\left(\xi\Gamma\frac{\partial\psi}{\partial\xi}\right),
\label{eq:moving}
\end{equation}
水分取 $\psi=C,\Gamma=D$，温度取 $\psi=T,\Gamma=k$，温度方程左侧采用题给有效储热系数 $\rho c_p$。因此，收缩效应主要通过内部扩散项的 $R^{-2}$ 和表面交换项的 $R^{-1}$ 进入模型。均匀收缩是本文的建模假设，实际材料的局部变形和裂纹未在本模型中考虑。

表面边界也必须同步变换。由 $\partial/\partial r=R^{-1}\partial/\partial\xi$ 得
\begin{align}
-\frac{k}{R}\left.\frac{\partial T}{\partial\xi}\right|_{\xi=1}
&=h_T(T_s-T_e),\label{eq:movingheatbc}\\
-\frac{D}{R}\left.\frac{\partial C}{\partial\xi}\right|_{\xi=1}
&=h_m(C_s-C_e).
\label{eq:movingmassbc}
\end{align}
因此在无量纲控制体方程中，内部扩散导通系数乘 $R^{-2}$，表面对流导通系数乘 $R^{-1}$。若只缩放网格而遗漏这些尺度因子，模型将不能正确反映半径变化带来的通量放大。

\subsection{模型的求解}

\subsubsection{材料坐标离散与迭代流程}

在 $\xi_i=1-(1-i/160)^2$ 上采用 161 个表面加密节点，时间步长取 $1$~s。每个时间层先由 PCHIP 更新半径和环境边界，再更新 $R^{-2}$ 内部扩散项及 $R^{-1}$ 表面交换项；随后采用 Picard 迭代依次求解温度场和水分场。全域最大水分浓度首次低于 $0.15$~kg/kg 时停止，并对相邻时间层线性插值定位达标时刻。计算流程见\cref{fig:moving}。

\begin{figure}[!htbp]
\centering
\includegraphics[height=0.72\textheight]{moving_boundary_solver.pdf}
\caption{问题四移动边界全隐式迭代流程}\label{fig:moving}
\end{figure}



为分离几何收缩作用，另保持附件四物性和环境不变，将半径固定为 2~cm，按相同步长与首达判据求解对照方案。

\subsubsection{物理位置输出规则}

计算解首先定义在固定材料坐标 $\xi$ 上，而题目模板给出的列对应固定物理位置 $r_j=0,0.1,\ldots,1.9$~cm。对任一时刻，仅当 $r_j\le R(t)$ 时计算
\begin{equation}
C(r_j,t)=\mathcal I_\xi[C(\xi,t)]\big|_{\xi=r_j/R(t)},
\label{eq:physicaloutput}
\end{equation}
其中 $\mathcal I_\xi$ 直接使用全部 161 个计算节点作线性插值；当 $r_j>R(t)$ 时，该位置已经处于药材外部，单元格留空而不是填 0。最后一列始终记录 $C(\xi=1,t)$，即随半径移动的实时表面。

该规则区分固定物理位置和随材料移动的表面位置，避免将药材外部的环境量误认为材料状态。

\subsubsection{坐标实现的交叉验证}

为检验固定材料坐标实现的可靠性，另在物理坐标中建立移动控制体模型。两种方法使用相同物性、环境、时间步长和首达判据独立计算，所得水分轨迹和达标时间在图示精度下重合，详细对照见附录。

\subsection{结果的分析和验证}

\subsubsection{达标时间与浓度分布}

问题四继续以全域最大水分浓度作为终止量。全部剖面保持中心最大，故事件定位仍可用\cref{eq:eventinterp,eq:profileinterp}。计算得到
\begin{equation}
t_d=183915.7175~\mathrm{s}=51.0877~\mathrm{h},\qquad
R(t_d)=1.2000~\mathrm{cm}.
\label{eq:q4time}
\end{equation}
典型时刻的固定物理位置与动态表面水分见\cref{tab:q4}。6~h 时中心水分仍为 1.7188，动态表面已降至 $0.4205~\mathrm{kg/kg}$；24~h 时中心为 0.2851，表面只有 0.0673；达标时中心为 0.1500，表面为 0.0526。

\begin{table}[!htbp]
\centering
\caption{问题四典型时刻水分浓度（kg/kg）}\label{tab:q4}
\begin{tabular}{ccccc}
\toprule
$t$/h&0 cm&0.5 cm&1.0 cm&动态表面\\
\midrule
6&1.7188&1.5370&1.0221&0.4205\\
12&0.7374&0.6544&0.4082&0.1669\\
18&0.4087&0.3686&0.2397&0.0892\\
24&0.2851&0.2610&0.1790&0.0673\\
30&0.2263&0.2094&0.1492&0.0595\\
36&0.1928&0.1796&0.1316&0.0560\\
42&0.1712&0.1604&0.1200&0.0541\\
48&0.1561&0.1468&0.1116&0.0530\\
51.0877&0.1500&0.1413&0.1081&0.0526\\
\bottomrule
\end{tabular}
\end{table}

动态表面在 $12.7848$~h 已达到 0.15，而中心需要 $51.0877$~h，两者仍相差 $38.3029$~h。收缩虽然显著缩短了内部扩散距离，却没有消除“外干内湿”的过程特征，因此实际控制仍应关注中心或内部代理量。

\subsubsection{收缩效应的反事实分解}

问题三和问题四的物性函数不同，直接比较 $57.4719$~h 与 $51.0877$~h 不能将全部差异归因于收缩。为保持物性和环境相同，另作两组对照：
\begin{enumerate}
  \item 使用附件四物性和相同环境，但保持 $R=2$~cm，得到固定半径反事实时长 $129.8445$~h；
  \item 保留真实 $R(t)$，采用原固定空间标量方程的速度描述，得到 $52.6375$~h，仅用作模型结构比较。
\end{enumerate}

第一组在本文材料收缩闭合下给出
\begin{equation}
\Delta t_R=129.8445-51.0877=78.7568~\mathrm{h},\qquad
\eta_R=\frac{\Delta t_R}{129.8445}=60.65\%.
\label{eq:q4reduction}
\end{equation}
第二组与材料模型相差 $1.5498$~h，表明速度描述影响长期预测。该差异不能解释为独立“坐标移动效应”，也不能据此认定任一方程已被实验验证。固定半径与均匀收缩模型的中心水分轨迹及半径变化见\cref{fig:q4}。

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.92\textwidth]{q4_shrinkage_comparison.pdf}
\caption{问题四收缩与固定半径反事实比较}\label{fig:q4}
\end{figure}

\subsubsection{收缩材料域与截面水分的联合展示}

\cref{fig:q4field}(a)给出收缩边界与水分场；(b)为径向解的轴对称截面重建，各截面共用长度比例和色标。几何收缩先于深部水分达标，重建截面不属于独立二维计算。

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.96\textwidth]{q4_shrinking_field.pdf}
\caption{问题四收缩材料域的水分时空场及等比例截面重建}\label{fig:q4field}
\end{figure}

\subsubsection{COMSOL 有限元复核}

COMSOL 采用相同物性、环境与半径历史，并设置轴向均匀、端面绝热。\cref{fig:q4comsol}(a)比较径向剖面，(b)给出六时刻的采样点对照，(c)展示半径历程。最大水分差为 $0.01162~\mathrm{kg/kg}$，位于近表层；最大温差为 $6.93\times10^{-5}~{}^\circ\mathrm C$。主模型首达时刻 51.0877~h 对应 COMSOL 中心水分 0.150036，支持整体趋势；该值不是 COMSOL 独立首达时间，近表层偏差仍需网格检验。

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.96\textwidth]{q4_comsol_profiles_validation.pdf}
\caption{COMSOL 有限元剖面、原有限体积结果校核与半径收缩历程}\label{fig:q4comsol}
\end{figure}

\input{contents/sections/q4_moving_mesh_validation.tex}

\subsubsection{空间网格与时间步长检验}

按问题三的加密方案对收缩模型复算，结果见\cref{tab:q4verify}。161 至 321 节点的达标时间相对变化为 $5.77\times10^{-7}$，2~s 至 1~s 的变化为 $1.29\times10^{-5}$，满足预设容差。累计归一化水分方程残差为 $1.77\times10^{-13}$，有效热方程最大单步残差为 $3.21\times10^{-14}$；后者仅针对不含蒸发潜热的既定温度方程。

\begin{table}[!htbp]
\centering
\caption{问题四网格与步长收敛检验：达标时间（h）}\label{tab:q4verify}
\begin{tabular}{lc}
\toprule
计算设置 & 问题四\\
\midrule
41 节点，10 s & 51.093100\\
81 节点，10 s & 51.093511\\
161 节点，10 s & 51.093626\\
321 节点，10 s & 51.093656\\
161 节点，2 s & 51.088358\\
161 节点，1 s（终算） & 51.087699\\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{端面影响的二维检验}

沿用问题一的半长圆柱轴对称模型，在侧表面与端面施加热、质 Robin 边界，并加入给定半径历史与均匀径向收缩。采用 30~s 步长，41$\times$21 节点下二维达标时间为 51.106259~h，与配对一维计算相对差为 $1.86\times10^{-7}$；加密至 81$\times$41 节点后为 51.106671~h，相对差为 $1.59\times10^{-7}$。

全二维最大水分均位于轴线与中截面交点。41$\times$21 网格下，全过程中截面最大温差为 $0.000108~{}^\circ\mathrm C$，最大水分差为 $1.38\times10^{-6}~\mathrm{kg/kg}$；最大端面与中截面水分差约为 $1.707~\mathrm{kg/kg}$。该检验支持当前端面条件下的中心控制，主模型仍采用 1~s 终算时长。

\subsubsection{本问结论与分析}

时间步与空间网格加密的达标时间相对差均小于 $1.3\times10^{-5}$；结合移动网格与有限元复核，数值结果支持收缩缩短扩散距离、加快整体干燥的判断。
