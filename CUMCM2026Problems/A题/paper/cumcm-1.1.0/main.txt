变物性与收缩边界下圆柱药材的热湿迁移建模
摘要
针对圆柱形药材烘干过程中的非稳态传热传质、物性随状态变化以及半径收缩问
题，本文建立一套逐级扩展的径向 Fourier–Fick 模型。利用保形分段三次 Hermite 插值
重构附件给出的环境温度、环境水分浓度与半径曲线；空间上采用节点中心有限体积法，
时间上采用全隐式格式，变物性方程以 Picard 迭代求解。完整逐时空结果分别写入题目
指定的四个工作簿。
针对问题一，在常物性条件下求解半径方向热湿迁移。1800 s 时，中心与表面温度
分别为 33.5769 ◦ C 和 36.7862 ◦ C，相应水分浓度分别为 2.5500 和 1.5121 kg/kg。结果显
示预热早期水分首先从表层快速逸出，而中心水分几乎保持初值。
针对问题二，将密度、比热、导热系数和扩散系数全部写为温度–水分浓度的状态
函数。3 h 时中心、表面温度分别达到 49.8494 ◦ C 和 49.9666 ◦ C，中心、表面水分浓度降
至 1.7662 和 1.0078 kg/kg。随干燥推进，径向温差显著缩小，但内部水分梯度仍然明显。
针对问题三，以 maxr C(r, t) ≤ 0.15 kg/kg 为全截面达标判据，并对跨越阈值的两
个时间层作线性首达插值。得到不考虑收缩时的干燥时长为 202270.75 s，即 56.1863 h；
达标瞬间中心浓度恰为 0.1500 kg/kg，表面浓度为 0.0525 kg/kg，控制位置始终位于圆
柱轴线。
针对问题四，引入 ξ = r/R(t) 固定域变换及 −(Ṙ/R)ξ 移动项，结合附件半径曲线计
算。得到收缩条件下干燥时长为 187148.57 s，即 51.9857 h，达标时半径为 1.2000 cm。与采
用附件四物性但固定半径的反事实模型相比，收缩使时长由 128.0711 h 缩短 59.41%；若
仅删去移动项则为 50.5467 h，说明几何扩散距离缩短占主导，而移动项产生约 1.4390 h
的抵消作用。
步长从 10 s 减半到 5 s 后，问题三、四干燥时长相对变化分别仅为 7.72 × 10−5
和 5.94 × 10−5 。二维轴对称核验中，中截面与一维模型的温度、水分最大差分别为
2.42 × 10−7 ◦ C 和 8.17 × 10−13 kg/kg；端面效应仍是模型推广至短柱或强端部换热场景
时应重点修正的因素。模型具有守恒、稳定、可复现和易扩展的特点。
关键字： 药材烘干 热湿耦合

有限体积法

1

移动边界

首达时间

一、 问题重述
一根长度为 25 cm、初始半径为 2 cm 的圆柱形药材置于烘干环境中，初温为 28 ◦ C，
初始水分浓度为 2.55 kg/kg。附件一给出前 4 h 的环境温度与环境水分浓度，附件二给
出 72 h 内半径的收缩记录。要求依次完成：
1. 在附件二所给常物性参数下，计算 0–1800 s 的温度与水分径向分布，并按 1 s、0.1 cm
输出完整结果；
2. 考虑物性随温度和水分变化，计算 0–3 h 的热湿分布；
3. 在不考虑收缩时，确定全体位置水分浓度不超过 0.15 kg/kg 的最短烘干时间；
4. 引入附件二的半径收缩与附件四物性，重新计算达标时长与动态空间分布。
问题的核心是处理圆柱坐标下的非稳态扩散、表面对流边界、非线性物性以及随时间变
化的计算区域，同时保证题目要求的高频输出可复现。

二、 问题分析
四问具有清晰的递进关系。问题一用于检验常物性 Fourier–Fick 方程及双 Robin 边
界；问题二只改变物性函数和环境时序，因此可沿用同一离散框架；问题三在问题二上
增加全域阈值首达判据；问题四再通过坐标变换把收缩区域映射到固定区间。由于长径
比为 25/(2 × 2) = 6.25，主模型取圆柱中截面的径向一维场，随后用二维轴对称模型专
门检查端面效应。
附件一、二的采样点不宜用高阶全局多项式连接，以免环境量和半径出现非物理振
荡，故采用保形分段三次 Hermite 插值（PCHIP）[3]。附件一在 4 h 后不再提供数据，本
文将末测环境温度与水分浓度保持不变；这是一项可复现且不引入额外经验参数的延拓
假设。总体技术路线如图 1 所示。
问题一
常物性预热

附件数据读取
PCHIP 环境边界

Fourier–Fick 方
程
双 Robin 边界

径向有限体积
全隐式迭代

问题二、三
变物性与首达判据

问题四
收缩移动边界

图1

四问递进建模与验证技术路线

2

步长收敛、二维核验
灵敏度与结果输出

三、 模型假设
1. 药材初始温度、水分浓度均匀，材料轴对称且无周向差异；主模型描述远离端面的
中截面径向传递。
2. 热量与水分仅通过扩散传递，忽略内部体热源、辐射、自然对流和重力导致的水分
迁移。
3. 表面传热、传质系数分别取附件二的 hT = 25 W/(m2 K)、hm = 8 × 10−7 m/s；因附
件三、四未另给系数，问题二至四沿用该值。
4. 温度方程不加入汽化潜热项。题目未给出蒸发潜热、平衡吸湿关系及热湿交叉项，若
强行加入将产生不可辨识参数；热湿联系通过状态相关的 ρ, cp , k, D 体现。
5. 问题四中长度保持不变，径向各点随半径均匀缩放；附件二半径先做单调 PCHIP 重
构。
6. 题中水分浓度按干基质量比理解，表面对流边界与附件环境水分浓度采用同一量纲。

四、 符号说明
表1

主要符号及含义

符号

含义

单位

备注

r, R

径向坐标、瞬时半径

m

0 ≤ r ≤ R(t)

T, Te

药材温度、环境温度

◦

C

温差与 K 相同

C, Ce

药材、环境水分浓度

kg/kg

干基质量比

ρ, cp

密度、比热容

kg/m3 , J/(kg K) 状态相关

k, D

导热系数、水分扩散系数

W/(m K), m2 /s

状态相关

hT , h m

表面传热、传质系数

2

W/(m K), m/s

Robin 边界

1

ξ = r/R(t)

s或h

max C ≤ 0.15

ξ

固定域坐标

td

全截面达标首达时刻

3

五、 模型建立与数值准备
5 . 1 控制方程与边界条件

在固定半径域内，径向传热与传质方程写为


∂T
∂T
1 ∂
ρ(C)cp (C)
rk(C)
,
=
∂t
r ∂r
∂r


∂C
∂C
1 ∂
=
rD(T, C)
.
∂t
r ∂r
∂r

(1)
(2)

中心对称条件与表面 Robin 条件分别为

−k

∂T
∂C
= 0,
= 0,
∂r r=0
∂r r=0
∂C
−D
= hm (CR − Ce ).
∂r r=R

∂T
= hT (TR − Te ),
∂r r=R

该形式直接表达控制体守恒，是有限体积离散的自然起点 [2]。
5 . 2 数据重构与物性函数

对附件一、二数据分别构造 PCHIP 函数 Te (t), Ce (t), R(t)。问题一采用
ρ = 820,

cp = 2600,

k = 0.36,

D = 7.0 × 10−9 exp(−0.89/C).

问题二、三采用
ρ = 650 + 128C,
k = 0.21 + 0.38

C
,
C +1

cp = 1450 + 2736

C
,
C +1

D = 2.4 × 10−3 exp(−0.45/C) exp[−3850/(T + 273.15)].

问题四采用
ρ = 760 + 90C,
k = 0.12 + 0.20

cp = 1850 + 2150
C
,
C +1

C
,
C +1

D = 4.2 × 10−4 exp(−0.30/C) exp[−3850/(T + 273.15)].

4

(3)
(4)

5 . 3 有限体积离散与非线性迭代
2
2
令相邻节点间距为 ∆r，第 i 个环形控制体单位长度体积为 Vi = π(ri+1/2
− ri−1/2
)。

以任一场量 ϕ 表示 T 或 C，全隐式离散为
si Vi n+1
n+1
(ϕ
− ϕni ) = Gi+1/2 (ϕn+1
) − Gi−1/2 (ϕn+1
− ϕn+1
i+1 − ϕi
i
i−1 ),
∆t i

(5)

其中温度场 si = ρi cp,i 、水分场 si = 1，Gf = 2πrf Γf /∆r，Γ = k 或 D；界面系数取相邻
节点算术平均。表面对流项直接并入末行对角元和右端项。每一时间层依次更新温度、
扩散系数和水分，并以
∥T (m+1) − T (m) ∥∞ < 10−8 ,

∥C (m+1) − C (m) ∥∞ < 10−10

作为 Picard 收敛条件。问题一、二用 ∆t = 1 s；问题三、四用 10 s 积分并按题意插值或
抽样输出。

六、 问题一：常物性预热过程
取 R = 0.02 m，在 r = 0, 0.001, . . . , 0.02 m 的 21 个节点上计算。初始扩散系数为
4.9377 × 10−9 m2 /s；以初值估计的热、质 Biot 数分别为 1.3889 和 3.2404，说明不可将内
部场简化为均匀块体。 图 2 显示表面温度随环境快速升高，水分则形成陡峭表层梯度。
表2

t/s

问题一典型时刻温度分布（◦ C）

0 cm

0.5 cm

1.0 cm

1.5 cm

2.0 cm

100 28.0001 28.0004 28.0041 28.0323 28.1786
300 28.0417 28.0644 28.1522 28.3678 28.8476
600 28.4553 28.5379 28.8053 29.3162 30.1638
900 29.3265 29.4603 29.8772 30.6172 31.7297
1200 30.5448

30.7117 31.2240

32.1139 33.4298

1500 31.9976 32.1886 32.7678 33.7483 35.1219
1800 33.5769 33.7734 34.3652 35.3629 36.7862
完整 0–1800 s、0.1 cm 网格结果见结果一工作簿。

5

表3

问题一典型时刻水分浓度（kg/kg）

t/s

0 cm

0.5 cm

1.0 cm

1.5 cm

2.0 cm

100 2.5500 2.5500 2.5500 2.5500 2.2886
300 2.5500 2.5500 2.5500 2.5486 2.0664
600 2.5500 2.5500 2.5500 2.5341 1.8848
900 2.5500 2.5500 2.5495 2.5040 1.7597
1200 2.5500 2.5500 2.5478 2.4647 1.6620
1500 2.5500 2.5499 2.5439 2.4212 1.5813
1800 2.5500 2.5496 2.5376 2.3763 1.5121
100 s
600 s

1200 s

1800 s

100 s
600 s

1200 s

1800 s

1.00

1.50

2.6
36

水分浓度 / (kg·kg⁻¹)

2.4

温度 / °C

34

32

30

2.2
2.0
1.8
1.6

28

1.4
0.00

0.25

0.50

0.75

1.00

1.25

1.50

1.75

2.00

0.00

0.25

0.50

0.75

1.25

到药材中心的距离 / cm

到药材中心的距离 / cm

(a)

(b)

图2

1.75

2.00

问题一温度与水分径向剖面

七、 问题二：变物性热湿迁移
将附件三物性函数代入 (1) 和 (2)，每个时间层最多 3 次 Picard 迭代即收敛。计算结
果列于表 (4) 和 (5)。 由图 3 可见，3 h 时温度已接近环境值，而水分仍呈现明显的内高
表4

t/h

0 cm

问题二温度分布（◦ C）

0.5 cm

1.0 cm

1.5 cm

2.0 cm

0.5 32.1912 32.3840 32.9675 33.9621 35.4148
1.0 40.3824 40.5544 41.0613 41.8791

43.0011

1.5 45.8467 45.9348 46.1931 46.6055 47.1403
2.0 48.4500 48.4878 48.5983 48.7735 49.0034
2.5 49.4668 49.4790 49.5134 49.5646 49.6613
3.0 49.8494 49.8553 49.8746 49.9101 49.9666

6

表5

t/h

问题二水分浓度分布（kg/kg）

0 cm

0.5 cm

1.0 cm

1.5 cm

2.0 cm

0.5 2.5499 2.5487 2.5249 2.3267 1.6485
1.0 2.5249 2.4940 2.3577 2.0234 1.4705
1.5 2.3854 2.3252 2.1344 1.8020 1.3471
2.0 2.1706 2.1083 1.9236 1.6258 1.2307
2.5 1.9566 1.9006 1.7359 1.4718

1.1163

3.0 1.7662 1.7166 1.5702 1.3331 1.0078
外低分布，说明后续干燥时长主要由内部传质控制。
0.5 h

1.5 h

3.0 h

0.5 h

50.0

3.0 h

2.50

水分浓度 / (kg·kg⁻¹)

47.5
45.0

温度 / °C

1.5 h

42.5
40.0
37.5
35.0

2.25
2.00
1.75
1.50
1.25

32.5

1.00
0.00

0.25

0.50

0.75

1.00

1.25

1.50

1.75

2.00

0.00

0.25

0.50

0.75

1.00

1.25

到药材中心的距离 / cm

到药材中心的距离 / cm

(a)

(b)

图3

1.50

1.75

2.00

问题二变物性温度与水分剖面

八、 问题三：全截面达标时间
定义首达时刻
td = inf{t ≥ 0 : max C(r, t) ≤ 0.15}.
0≤r≤R

(6)

数值场在整个过程中均满足 C(ri , t) ≥ C(ri+1 , t)，故中心是控制点。当相邻时间层的中
心浓度跨越 0.15 时，对中心浓度作线性插值，同时对全剖面插值，得到
td = 202270.7481 s = 56.1863 h.
图 4 同时给出控制点时间历程和径向剖面。达标时表面已远低于阈值，若只用表面传
感器判停会显著低估内部所需干燥时间。

7

表6

问题三每 6 h 及达标时的水分浓度（kg/kg）

t/h

0 cm

0.5 cm

1.0 cm

1.5 cm

2.0 cm

6 1.0159 0.9871 0.9008 0.7548 0.5335
12 0.4551 0.4421 0.4022 0.3292 0.1646
18 0.2979 0.2906 0.2677 0.2239 0.0852
24 0.2367 0.2316 0.2153 0.1835 0.0666
30 0.2045 0.2005 0.1876 0.1618 0.0597
36 0.1845

0.1811 0.1702 0.1480 0.0564

42 0.1707 0.1677 0.1580 0.1384 0.0546
48 0.1604 0.1577 0.1490 0.1312 0.0535
54 0.1525 0.1500 0.1420 0.1255 0.0528
56.1863 0.1500 0.1476 0.1398 0.1238 0.0525
6h
18 h
中心
表面
阈值 0.15

2.0
1.5
1.0
0.5

56.186 h

1.0

水分浓度 / (kg·kg⁻¹)

水分浓度 / (kg·kg⁻¹)

2.5

36 h
48 h

0.8
0.6
0.4
0.2

56.186 h

0.0

0.0
0

10

20

30

40

50

0.00

0.25

0.50

0.75

1.00

1.25

时间 / h

到药材中心的距离 / cm

(a)

(b)

图4

1.50

1.75

2.00

问题三阈值首达过程与径向分布

九、 问题四：收缩移动边界模型
9 . 1 固定域变换

令 ξ = r/R(t) ∈ [0, 1]，对 ϕ = T, C 有
∂ϕ
Ṙ ∂ϕ
∂ϕ
=
− ξ .
∂t r
∂t ξ R ∂ξ
因此固定域方程统一写成
∂ϕ Ṙ ∂ϕ
1 ∂
− ξ
= 2
∂t
R ∂ξ
R ξ ∂ξ

8



∂ϕ
ξΓ
∂ξ


,

(7)

其中温度方程左、右两侧还应分别乘以 ρcp 并将 Γ 换为 k。因 Ṙ < 0，移动速度 uξ =
−(Ṙ/R)ξ ≥ 0，该项以一阶迎风方式隐式处理；最大 Courant 数仅为 0.00838。计算流程
见图 5。
初始化 T、C、R

PCHIP 更新环境量与半径
映射 ξ=r/R(t)

以前一迭代场计算
ρ、cₚ、k、D 及界面系数

否
组装全隐式有限体积方程
含 ( /R)ξ 移动项并求解

−Ṙ

否

Picard 收敛？

是
推进时间并记录剖面

max C ≤ 0.15？

是
线性插值首达时刻
输出结果与误差核验

图5

问题四移动边界全隐式迭代流程

9 . 2 结果及收缩作用分解

得到
td = 187148.5658 s = 51.9857 h,
9

R(td ) = 1.2000 cm.

固定物理位置只有在 r ≤ R(t) 时才有定义，因而完整结果四工作簿中，已落到药材外部
的固定半径列留空，并另设“动态表面”列。为避免把物性差异误判为收缩效应，另以
表7

问题四典型时刻水分浓度（kg/kg）

t/h

0 cm

0.5 cm

1.0 cm 动态表面

6 1.9229 1.7535 1.2333

0.5549

12 0.8581 0.7631 0.4784

0.2099

18 0.4534 0.4078 0.2619

0.1008

24 0.3042 0.2777 0.1876

0.0709

30 0.2359 0.2177 0.1530

0.0607

36 0.1983 0.1843 0.1333

0.0564

42 0.1746 0.1632 0.1206

0.0543

48 0.1583 0.1486

0.1116

0.0530

0.1411 0.1070

0.0525

51.9857 0.1500

附件四物性计算固定 R = 2 cm 的反事实方案，得 128.0711 h；收缩方案缩短 76.0854 h，
即 59.41%。若保持收缩几何但删去 (7) 的移动项，时长为 50.5467 h，说明扩散距离缩
短是主要加速机制，而材料坐标迁移抵消约 1.4390 h。对比如图 6 所示。
2.0

固定半径对照（附录 4 物性）
收缩模型（问题 4）

2.0

1.8

药材半径 / cm

中心水分浓度 / (kg·kg⁻¹)

2.5

1.5
1.0
0.5

1.6

1.4

1.200 cm

51.986 h
1.2

0.0
0

20

40

60

80

100

120

0

10

20

30

时间 / h

时间 / h

(a)

(b)

图6

40

50

问题四收缩与固定半径反事实比较

十、 模型检验、评价与推广
10 . 1 时间步长与二维核验

步长减半结果表明首达时间已收敛。二维轴对称有限体积计算进一步表明，一维模
型与二维中截面高度一致；但端面差异不可忽略，因此本文结果应解释为中截面径向场，
不能直接用于预测端部局部质量。
10

表8

主要数值检验结果

检验项目

基准值

误差或变化

问题三：∆t = 10 s 与 5 s

56.1863 h / 56.1820 h

7.72 × 10−5

问题四：∆t = 10 s 与 5 s

51.9857 h / 51.9826 h

5.94 × 10−5

二维中截面温度与一维模型

—

2.42 × 10−7 ◦ C

二维中截面水分与一维模型

— 8.17 × 10−13 kg/kg

二维端面–中截面最大温差

—

3.6576 ◦ C

二维端面–中截面最大水分差

—

0.8567 kg/kg

10 . 2 灵敏度分析

分别将 4 h 后保持的末端环境温度和环境水分浓度改变 ±5%，得到表 9。温度是更
敏感的控制量；问题三、四对同向扰动的响应一致，支持模型比较的稳健性。
表9

环境参数扰动下的干燥时长（h）

情形

问题三

问题四

基准

56.1863 51.9857

末端环境温度 −5% 60.8613 56.2440
末端环境温度 +5%

52.0215 48.1834

末端环境水分 −5% 56.0609 51.8907
末端环境水分 +5%

56.3323 52.0960

10 . 3 优缺点与推广

模型优点是控制体守恒、全隐式稳定、四问共享统一框架，且能够直接生成题目要
求的逐时空结果；PCHIP 保留了附件序列的单调或局部形态，首达插值避免将输出间隔
误当成物理精度。局限在于未标定潜热、平衡吸湿与材料各向异性，4 h 后环境采用常
值延拓，且主模型未描述端面。
若获得表面蒸发热、平衡含湿曲线和内部温湿传感数据，可把本模型推广为 Luikov
型交叉耦合方程 [4]，并通过参数反演估计 hT , hm , D；若药材长度较短，则应直接采用
本文核验所用二维轴对称框架。工业应用中可将中心浓度代理模型嵌入温度控制器，以
“全截面首达而不过度干燥”为优化目标。

11

十一、 人工智能工具使用说明
本文建模过程中使用了生成式人工智能工具辅助整理题意、比较候选离散方案、检
查论文结构和润色文字。用户提供的共享对话链接被用作“Fourier–Fick 方程、有限体
积全隐式离散、首达判据、固定域移动边界及二维核验”这一总体思路的参考。所有附
件数据均由程序直接读取，控制方程、边界符号、程序实现、四份工作簿、图表和数值
结论均在本地独立计算；并通过步长减半、二维模型、物理单调性和文件回读进行复核。
人工智能未代替实测数据，也未虚构外部实验结论。

参考文献
[1] Crank J. The Mathematics of Diffusion[M]. 2nd ed. Oxford: Clarendon Press, 1975.
[2] Patankar S V. Numerical Heat Transfer and Fluid Flow[M]. Washington, DC: Hemisphere,
1980.
[3] Fritsch F N, Carlson R E. Monotone piecewise cubic interpolation[J]. SIAM Journal on
Numerical Analysis, 1980, 17(2): 238–246. DOI: 10.1137/0717021.
[4] Luikov A V. Systems of differential equations of heat and mass transfer in capillary-porous
bodies[J]. International Journal of Heat and Mass Transfer, 1975, 18(1): 1–14.
[5] Adrover A, Brasiello A. A moving boundary model for food isothermal drying
and shrinkage[J]. Journal of Food Process Engineering, 2019, 42(7): e13178. DOI:
10.1111/jfpe.13178.

12

附录 A

文件清单与复现入口

表 10

提交文件及作用

文件

作用

result1.xlsx

问题一每 1 s、0.1 cm 的温度和水分结果

result2.xlsx

问题二每 1 s、0.1 cm 的温度和水分结果

result3.xlsx

问题三每 60 s、0.1 cm 的水分结果

result4.xlsx

问题四每 60 s、固定位置及动态表面结果

solve_drying.py

四问求解、阈值判定与步长检验主程序

write_results.py

结果工作簿生成与回读验证

validate_2d.py

二维轴对称核验程序

run_sensitivity.py 环境参数灵敏度分析程序
论文数据图复现程序

make_figures.py

在项目根目录执行 python code/solve_drying.py，再依次执行其余脚本即可复现
JSON/NPZ 中间结果、四个工作簿和全部图件。程序使用固定网格与确定性线性代数，
不含随机过程。

附录 B

核心源程序

限于版面，附录以可直接编译嵌入的方式给出求解主程序全文。
"""Numerical solution for CUMCM 2026 Problem A.
The model uses a node-centred finite-volume discretisation in the radial
coordinate, fully implicit diffusion, Picard iteration for nonlinear
properties, and an ALE-style fixed-domain transform for shrinkage.
"""
from __future__ import annotations
import argparse
import json
import math
import sys
from dataclasses import dataclass
from pathlib import Path
from typing import Callable
import numpy as np

13

from openpyxl import load_workbook
from scipy.interpolate import PchipInterpolator
from scipy.linalg import solve_banded

ROOT = Path(__file__).resolve().parents[1]
INPUT_DIR = ROOT / "附件"
RESULTS_DIR = ROOT / "results"
H_T = 25.0
H_M = 8.0e-7
R_FIXED = 0.02
NODES = 21
INITIAL_T = 28.0
INITIAL_C = 2.55
THRESHOLD_C = 0.15

@dataclass
class Environment:
time_s: np.ndarray
temperature_c: np.ndarray
moisture: np.ndarray
temp_interp: PchipInterpolator
moisture_interp: PchipInterpolator
def values(
self,
time_s: float,
terminal_temp_scale: float = 1.0,
terminal_moisture_scale: float = 1.0,
) -> tuple[float, float]:
cutoff = float(self.time_s[-1])
if time_s <= cutoff:
return float(self.temp_interp(time_s)), float(self.moisture_interp(time_s))
return (
float(self.temperature_c[-1] * terminal_temp_scale),
float(self.moisture[-1] * terminal_moisture_scale),
)

@dataclass
class RadiusHistory:
time_s: np.ndarray
radius_m: np.ndarray
interp: PchipInterpolator
derivative: PchipInterpolator
monotone_adjustments: int

14

def values(self, time_s: float) -> tuple[float, float]:
bounded = min(max(time_s, float(self.time_s[0])), float(self.time_s[-1]))
return float(self.interp(bounded)), float(self.derivative(bounded))

def _read_numeric_sheet(path: Path) -> np.ndarray:
book = load_workbook(path, read_only=True, data_only=True)
sheet = book.active
rows: list[list[float]] = []
for row in sheet.iter_rows(min_row=2, values_only=True):
if row[0] is None:
continue
rows.append([float(value) for value in row])
return np.asarray(rows, dtype=float)

def load_environment() -> Environment:
data = _read_numeric_sheet(INPUT_DIR / "附件1.xlsx")
time_s, temperature_c, moisture = data.T
if not np.all(np.diff(time_s) > 0):
raise ValueError("附件1时间列不是严格递增。")
if not np.all(np.isfinite(data)):
raise ValueError("附件1含非有限数。")
return Environment(
time_s=time_s,
temperature_c=temperature_c,
moisture=moisture,
temp_interp=PchipInterpolator(time_s, temperature_c),
moisture_interp=PchipInterpolator(time_s, moisture),
)

def load_radius_history() -> RadiusHistory:
data = _read_numeric_sheet(INPUT_DIR / "附件2.xlsx")
time_s = data[:, 0]
raw_radius_m = data[:, 1] / 100.0
if not np.all(np.diff(time_s) > 0):
raise ValueError("附件2时间列不是严格递增。")
monotone_radius_m = np.minimum.accumulate(raw_radius_m)
adjustments = int(np.count_nonzero(np.abs(monotone_radius_m - raw_radius_m) > 1e-12))
interp = PchipInterpolator(time_s, monotone_radius_m, extrapolate=False)
return RadiusHistory(
time_s=time_s,
radius_m=monotone_radius_m,
interp=interp,
derivative=interp.derivative(),

15

monotone_adjustments=adjustments,
)

def property_q1(temperature_c: np.ndarray, moisture: np.ndarray) -> tuple[np.ndarray, ...]:
del temperature_c
rho = np.full_like(moisture, 820.0)
cp = np.full_like(moisture, 2600.0)
k = np.full_like(moisture, 0.36)
safe_c = np.maximum(moisture, 1e-8)
diffusivity = 7.0e-9 * np.exp(-0.89 / safe_c)
return rho, cp, k, diffusivity

def property_q23(temperature_c: np.ndarray, moisture: np.ndarray) -> tuple[np.ndarray, ...]:
safe_c = np.maximum(moisture, 1e-8)
rho = 650.0 + 128.0 * safe_c
cp = 1450.0 + 2736.0 * safe_c / (safe_c + 1.0)
k = 0.21 + 0.38 * safe_c / (safe_c + 1.0)
diffusivity = (
2.4e-3
* np.exp(-0.45 / safe_c)
* np.exp(-3850.0 / (temperature_c + 273.15))
)
return rho, cp, k, diffusivity

def property_q4(temperature_c: np.ndarray, moisture: np.ndarray) -> tuple[np.ndarray, ...]:
safe_c = np.maximum(moisture, 1e-8)
rho = 760.0 + 90.0 * safe_c
cp = 1850.0 + 2150.0 * safe_c / (safe_c + 1.0)
k = 0.12 + 0.20 * safe_c / (safe_c + 1.0)
diffusivity = (
4.2e-4
* np.exp(-0.30 / safe_c)
* np.exp(-3850.0 / (temperature_c + 273.15))
)
return rho, cp, k, diffusivity

def face_interpolate(left: np.ndarray, right: np.ndarray) -> np.ndarray:
"""Linear face interpolation for a smooth state-dependent coefficient."""
return 0.5 * (left + right)

def node_control_volumes(coordinate: np.ndarray) -> tuple[np.ndarray, np.ndarray]:
"""Return dimensionless/physical radial CV volumes and internal face radii."""

16

faces = 0.5 * (coordinate[:-1] + coordinate[1:])
lower = np.concatenate(([0.0], faces))
upper = np.concatenate((faces, [coordinate[-1]]))
volumes = math.pi * (upper**2 - lower**2)
return volumes, faces

def implicit_radial_step(
old: np.ndarray,
storage: np.ndarray,
conductivity: np.ndarray,
dt: float,
coordinate: np.ndarray,
diffusion_scale: float,
boundary_conductance: float,
boundary_value: float,
advection_velocity: np.ndarray | None = None,
) -> np.ndarray:
"""One implicit node-centred radial FVM step on coordinate [0, 1] or [0, R]."""
count = len(old)
volumes, faces = node_control_volumes(coordinate)
delta = float(coordinate[1] - coordinate[0])
face_property = face_interpolate(conductivity[:-1], conductivity[1:])
conductance = 2.0 * math.pi * faces * face_property * diffusion_scale / delta
capacity = storage * volumes / dt
diagonal = capacity.copy()
lower = np.zeros(count - 1)
upper = np.zeros(count - 1)
rhs = capacity * old
diagonal[:-1] += conductance
diagonal[1:] += conductance
upper -= conductance
lower -= conductance
diagonal[-1] += boundary_conductance
rhs[-1] += boundary_conductance * boundary_value
if advection_velocity is not None:
# PDE form: phi_t + u(xi) phi_xi = diffusion; u >= 0 under shrinkage.
advective = storage[1:] * volumes[1:] * np.maximum(advection_velocity[1:], 0.0) / delta
diagonal[1:] += advective
lower -= advective
matrix = np.zeros((3, count))
matrix[0, 1:] = upper

17

matrix[1, :] = diagonal
matrix[2, :-1] = lower
return solve_banded((1, 1), matrix, rhs, check_finite=False)

def advance_coupled(
temperature_old: np.ndarray,
moisture_old: np.ndarray,
dt: float,
radius_m: float,
radius_rate_m_s: float,
ambient_temperature_c: float,
ambient_moisture: float,
property_model: Callable[[np.ndarray, np.ndarray], tuple[np.ndarray, ...]],
moving_domain: bool,
tolerance_temperature: float = 1e-8,
tolerance_moisture: float = 1e-10,
max_iterations: int = 30,
) -> tuple[np.ndarray, np.ndarray, int]:
temperature = temperature_old.copy()
moisture = moisture_old.copy()
if moving_domain:
coordinate = np.linspace(0.0, 1.0, len(temperature_old))
diffusion_scale = 1.0 / radius_m**2
boundary_scale = 1.0 / radius_m
shrink_velocity = -(radius_rate_m_s / radius_m) * coordinate
else:
coordinate = np.linspace(0.0, radius_m, len(temperature_old))
diffusion_scale = 1.0
boundary_scale = 1.0
shrink_velocity = None
for iteration in range(1, max_iterations + 1):
rho, cp, k, _ = property_model(temperature, moisture)
heat_storage = rho * cp
heat_boundary = 2.0 * math.pi * coordinate[-1] * H_T * boundary_scale
next_temperature = implicit_radial_step(
old=temperature_old,
storage=heat_storage,
conductivity=k,
dt=dt,
coordinate=coordinate,
diffusion_scale=diffusion_scale,
boundary_conductance=heat_boundary,
boundary_value=ambient_temperature_c,
advection_velocity=shrink_velocity,
)

18

_, _, _, diffusivity = property_model(next_temperature, moisture)
mass_boundary = 2.0 * math.pi * coordinate[-1] * H_M * boundary_scale
next_moisture = implicit_radial_step(
old=moisture_old,
storage=np.ones_like(moisture),
conductivity=diffusivity,
dt=dt,
coordinate=coordinate,
diffusion_scale=diffusion_scale,
boundary_conductance=mass_boundary,
boundary_value=ambient_moisture,
advection_velocity=shrink_velocity,
)
error_t = float(np.max(np.abs(next_temperature - temperature)))
error_c = float(np.max(np.abs(next_moisture - moisture)))
temperature = next_temperature
moisture = next_moisture
if error_t < tolerance_temperature and error_c < tolerance_moisture:
break
else:
raise RuntimeError(
f"Picard iteration failed: dT={error_t:.3e}, dC={error_c:.3e}, dt={dt}"
)
if not np.all(np.isfinite(temperature)) or not np.all(np.isfinite(moisture)):
raise FloatingPointError("Non-finite state generated.")
if np.min(moisture) < -1e-10 or np.max(moisture) > INITIAL_C + 1e-8:
raise FloatingPointError("Moisture left physical bounds.")
return temperature, moisture, iteration

def simulate_fixed(
environment: Environment,
property_model: Callable[[np.ndarray, np.ndarray], tuple[np.ndarray, ...]],
end_time_s: float | None,
dt_s: float,
output_interval_s: float,
stop_at_threshold: bool,
terminal_temp_scale: float = 1.0,
terminal_moisture_scale: float = 1.0,
) -> dict[str, np.ndarray | float | int | bool]:
temperature = np.full(NODES, INITIAL_T)
moisture = np.full(NODES, INITIAL_C)
radial_m = np.linspace(0.0, R_FIXED, NODES)
times = [0.0]

19

temperatures = [temperature.copy()]
moistures = [moisture.copy()]
iterations: list[int] = []
monotone_profiles = True
center_is_max = True
previous_max = float(np.max(moisture))
previous_time = 0.0
threshold_time = math.nan
threshold_temperature: np.ndarray | None = None
threshold_moisture: np.ndarray | None = None
stop_output_time = math.inf
time_s = 0.0
next_output = output_interval_s
maximum_time = float(end_time_s if end_time_s is not None else 10.0 * 24.0 * 3600.0)
while time_s < maximum_time - 1e-9:
step = min(dt_s, maximum_time - time_s)
new_time = time_s + step
temperature_before = temperature.copy()
moisture_before = moisture.copy()
ambient_t, ambient_c = environment.values(
new_time, terminal_temp_scale, terminal_moisture_scale
)
temperature, moisture, count = advance_coupled(
temperature,
moisture,
step,
R_FIXED,
0.0,
ambient_t,
ambient_c,
property_model,
moving_domain=False,
)
iterations.append(count)
time_s = new_time
current_max = float(np.max(moisture))
monotone_profiles = monotone_profiles and bool(np.all(np.diff(moisture) <= 2e-9))
center_is_max = center_is_max and bool(
moisture[0] >= float(np.max(moisture)) - 1e-9
)
if previous_max > THRESHOLD_C >= current_max and math.isnan(threshold_time):
fraction = (previous_max - THRESHOLD_C) / max(previous_max - current_max, 1e-15)
threshold_time = previous_time + fraction * step
threshold_temperature = (
temperature_before + fraction * (temperature - temperature_before)

20

)
threshold_moisture = moisture_before + fraction * (moisture - moisture_before)
stop_output_time = (
math.ceil(threshold_time / output_interval_s) * output_interval_s
)
if time_s + 1e-8 >= next_output:
times.append(time_s)
temperatures.append(temperature.copy())
moistures.append(moisture.copy())
next_output += output_interval_s
if stop_at_threshold and time_s + 1e-8 >= stop_output_time:
break
previous_max = current_max
previous_time = time_s
return {
"times_s": np.asarray(times),
"radius_m": radial_m,
"temperature_c": np.asarray(temperatures),
"moisture": np.asarray(moistures),
"threshold_time_s": threshold_time,
"threshold_temperature_c": threshold_temperature,
"threshold_moisture": threshold_moisture,
"last_time_s": time_s,
"picard_max": int(max(iterations, default=0)),
"picard_mean": float(np.mean(iterations) if iterations else 0.0),
"monotone_profiles": monotone_profiles,
"center_is_max": center_is_max,
}

def simulate_moving(
environment: Environment,
radius_history: RadiusHistory,
dt_s: float,
output_interval_s: float,
stop_at_threshold: bool = True,
terminal_temp_scale: float = 1.0,
terminal_moisture_scale: float = 1.0,
include_advection: bool = True,
) -> dict[str, np.ndarray | float | int | bool]:
xi = np.linspace(0.0, 1.0, NODES)
temperature = np.full(NODES, INITIAL_T)
moisture = np.full(NODES, INITIAL_C)
times = [0.0]

21

radii = [R_FIXED]
temperatures = [temperature.copy()]
moistures = [moisture.copy()]
iterations: list[int] = []
previous_max = float(np.max(moisture))
previous_time = 0.0
threshold_time = math.nan
threshold_temperature: np.ndarray | None = None
threshold_moisture: np.ndarray | None = None
threshold_radius_m = math.nan
stop_output_time = math.inf
time_s = 0.0
next_output = output_interval_s
maximum_time = float(radius_history.time_s[-1])
monotone_profiles = True
center_is_max = True
maximum_cfl = 0.0
while time_s < maximum_time - 1e-9:
step = min(dt_s, maximum_time - time_s)
new_time = time_s + step
temperature_before = temperature.copy()
moisture_before = moisture.copy()
radius_m, radius_rate_m_s = radius_history.values(new_time)
if not include_advection:
radius_rate_m_s = 0.0
ambient_t, ambient_c = environment.values(
new_time, terminal_temp_scale, terminal_moisture_scale
)
temperature, moisture, count = advance_coupled(
temperature,
moisture,
step,
radius_m,
radius_rate_m_s,
ambient_t,
ambient_c,
property_q4,
moving_domain=True,
)
iterations.append(count)
time_s = new_time
current_max = float(np.max(moisture))
maximum_cfl = max(
maximum_cfl,
float(abs(radius_rate_m_s / radius_m) * step / (xi[1] - xi[0])),
)

22

monotone_profiles = monotone_profiles and bool(np.all(np.diff(moisture) <= 2e-9))
center_is_max = center_is_max and bool(
moisture[0] >= float(np.max(moisture)) - 1e-9
)
if previous_max > THRESHOLD_C >= current_max and math.isnan(threshold_time):
fraction = (previous_max - THRESHOLD_C) / max(previous_max - current_max, 1e-15)
threshold_time = previous_time + fraction * step
threshold_temperature = (
temperature_before + fraction * (temperature - temperature_before)
)
threshold_moisture = moisture_before + fraction * (moisture - moisture_before)
threshold_radius_m = radius_history.values(threshold_time)[0]
stop_output_time = (
math.ceil(threshold_time / output_interval_s) * output_interval_s
)
if time_s + 1e-8 >= next_output:
times.append(time_s)
radii.append(radius_m)
temperatures.append(temperature.copy())
moistures.append(moisture.copy())
next_output += output_interval_s
if stop_at_threshold and time_s + 1e-8 >= stop_output_time:
break
previous_max = current_max
previous_time = time_s
return {
"times_s": np.asarray(times),
"xi": xi,
"radii_m": np.asarray(radii),
"temperature_c": np.asarray(temperatures),
"moisture": np.asarray(moistures),
"threshold_time_s": threshold_time,
"threshold_temperature_c": threshold_temperature,
"threshold_moisture": threshold_moisture,
"threshold_radius_m": threshold_radius_m,
"last_time_s": time_s,
"picard_max": int(max(iterations, default=0)),
"picard_mean": float(np.mean(iterations) if iterations else 0.0),
"monotone_profiles": monotone_profiles,
"center_is_max": center_is_max,
"maximum_advection_cfl": maximum_cfl,
}

23

def extract_fixed_table(
simulation: dict[str, np.ndarray | float | int | bool],
requested_times_s: list[float],
requested_radii_cm: list[float],
variable: str,
) -> list[list[float]]:
times = np.asarray(simulation["times_s"])
radii_m = np.asarray(simulation["radius_m"])
values = np.asarray(simulation[variable])
table: list[list[float]] = []
for requested_time in requested_times_s:
threshold_time = float(simulation.get("threshold_time_s", math.nan))
threshold_key = (
"threshold_temperature_c" if variable == "temperature_c" else "threshold_moisture"
)
threshold_profile = simulation.get(threshold_key)
if (
threshold_profile is not None
and math.isfinite(threshold_time)
and abs(requested_time - threshold_time) < 1e-5
):
profile = np.asarray(threshold_profile)
else:
time_index = int(np.argmin(np.abs(times - requested_time)))
profile = values[time_index]
row = [
float(np.interp(radius_cm / 100.0, radii_m, profile))
for radius_cm in requested_radii_cm
]
table.append(row)
return table

def extract_moving_profile(
simulation: dict[str, np.ndarray | float | int | bool],
requested_time_s: float,
requested_radii_cm: list[float],
) -> list[float | None]:
times = np.asarray(simulation["times_s"])
xi = np.asarray(simulation["xi"])
radii_m = np.asarray(simulation["radii_m"])
moisture = np.asarray(simulation["moisture"])
threshold_time = float(simulation.get("threshold_time_s", math.nan))
threshold_profile = simulation.get("threshold_moisture")
if (
threshold_profile is not None

24

and math.isfinite(threshold_time)
and abs(requested_time_s - threshold_time) < 1e-5
):
profile = np.asarray(threshold_profile)
radius = float(simulation["threshold_radius_m"])
else:
index = int(np.argmin(np.abs(times - requested_time_s)))
profile = moisture[index]
radius = float(radii_m[index])
values: list[float | None] = []
for radius_cm in requested_radii_cm:
position_m = radius_cm / 100.0
if position_m > radius + 1e-12:
values.append(None)
else:
values.append(float(np.interp(position_m / radius, xi, profile)))
values.append(float(profile[-1]))
return values

def save_npz(path: Path, simulation: dict[str, np.ndarray | float | int | bool]) -> None:
arrays = {key: value for key, value in simulation.items() if isinstance(value, np.ndarray)}
np.savez_compressed(path, **arrays)

def json_ready(value):
if isinstance(value, np.ndarray):
return value.tolist()
if isinstance(value, (np.floating, np.integer)):
return value.item()
if isinstance(value, float) and math.isnan(value):
return None
if isinstance(value, dict):
return {key: json_ready(item) for key, item in value.items()}
if isinstance(value, (list, tuple)):
return [json_ready(item) for item in value]
return value

def write_summary(path: Path, payload: dict) -> None:
path.write_text(
json.dumps(json_ready(payload), ensure_ascii=False, indent=2),
encoding="utf-8",
)

def run_all(run_convergence: bool = True) -> None:

25

RESULTS_DIR.mkdir(parents=True, exist_ok=True)
for name in ("q1", "q2", "q3", "q4"):
(RESULTS_DIR / name).mkdir(parents=True, exist_ok=True)
environment = load_environment()
radius_history = load_radius_history()
data_audit = {
"environment_rows": len(environment.time_s),
"environment_time_range_s": [environment.time_s[0], environment.time_s[-1]],
"environment_sampling_s": np.unique(np.diff(environment.time_s)),
"temperature_range_c": [
float(np.min(environment.temperature_c)),
float(np.max(environment.temperature_c)),
],
"ambient_moisture_range": [
float(np.min(environment.moisture)),
float(np.max(environment.moisture)),
],
"radius_rows": len(radius_history.time_s),
"radius_time_range_s": [radius_history.time_s[0], radius_history.time_s[-1]],
"radius_range_cm": [
float(np.min(radius_history.radius_m) * 100.0),
float(np.max(radius_history.radius_m) * 100.0),
],
"radius_monotone_adjustments": radius_history.monotone_adjustments,
}
write_summary(RESULTS_DIR / "data_audit.json", data_audit)
q1 = simulate_fixed(
environment,
property_q1,
end_time_s=1800.0,
dt_s=1.0,
output_interval_s=1.0,
stop_at_threshold=False,
)
save_npz(RESULTS_DIR / "q1" / "fields.npz", q1)
q1_times = [100, 300, 600, 900, 1200, 1500, 1800]
table_radii = [0.0, 0.5, 1.0, 1.5, 2.0]
q1_summary = {
"table_times_s": q1_times,
"table_radii_cm": table_radii,
"temperature_table_c": extract_fixed_table(q1, q1_times, table_radii, "temperature_c"),
"moisture_table": extract_fixed_table(q1, q1_times, table_radii, "moisture"),
"biot_heat": H_T * R_FIXED / 0.36,
"initial_diffusivity_m2_s": float(property_q1(
np.array([INITIAL_T]), np.array([INITIAL_C])

26

)[3][0]),
"biot_mass_initial": H_M * R_FIXED / float(property_q1(
np.array([INITIAL_T]), np.array([INITIAL_C])
)[3][0]),
"picard_max": q1["picard_max"],
"picard_mean": q1["picard_mean"],
"temperature_bounds_c": [
float(np.min(q1["temperature_c"])),
float(np.max(q1["temperature_c"])),
],
"moisture_bounds": [
float(np.min(q1["moisture"])),
float(np.max(q1["moisture"])),
],
}
write_summary(RESULTS_DIR / "q1" / "summary.json", q1_summary)
q2 = simulate_fixed(
environment,
property_q23,
end_time_s=10800.0,
dt_s=1.0,
output_interval_s=1.0,
stop_at_threshold=False,
)
save_npz(RESULTS_DIR / "q2" / "fields.npz", q2)
q2_times = [1800, 3600, 5400, 7200, 9000, 10800]
q2_summary = {
"table_times_h": [time / 3600.0 for time in q2_times],
"table_radii_cm": table_radii,
"temperature_table_c": extract_fixed_table(q2, q2_times, table_radii, "temperature_c"),
"moisture_table": extract_fixed_table(q2, q2_times, table_radii, "moisture"),
"picard_max": q2["picard_max"],
"picard_mean": q2["picard_mean"],
"monotone_profiles": q2["monotone_profiles"],
"center_is_max": q2["center_is_max"],
}
write_summary(RESULTS_DIR / "q2" / "summary.json", q2_summary)
q3 = simulate_fixed(
environment,
property_q23,
end_time_s=None,
dt_s=10.0,
output_interval_s=60.0,
stop_at_threshold=True,
)

27

save_npz(RESULTS_DIR / "q3" / "fields.npz", q3)
q3_end = float(q3["threshold_time_s"])
q3_report_times = list(np.arange(6 * 3600, math.floor(q3_end / (6 * 3600)) * 6 * 3600 + 1,
6 * 3600))
if not q3_report_times or abs(q3_report_times[-1] - q3_end) > 1e-6:
q3_report_times.append(q3_end)
q3_summary = {
"drying_time_s": q3_end,
"drying_time_h": q3_end / 3600.0,
"output_end_s": q3["last_time_s"],
"table_times_h": [time / 3600.0 for time in q3_report_times],
"table_radii_cm": table_radii,
"moisture_table": extract_fixed_table(q3, q3_report_times, table_radii, "moisture"),
"center_is_max": q3["center_is_max"],
"monotone_profiles": q3["monotone_profiles"],
"picard_max": q3["picard_max"],
"picard_mean": q3["picard_mean"],
}
q4 = simulate_moving(
environment,
radius_history,
dt_s=10.0,
output_interval_s=60.0,
stop_at_threshold=True,
)
save_npz(RESULTS_DIR / "q4" / "fields.npz", q4)
q4_end = float(q4["threshold_time_s"])
q4_report_times = list(np.arange(6 * 3600, math.floor(q4_end / (6 * 3600)) * 6 * 3600 + 1,
6 * 3600))
if not q4_report_times or abs(q4_report_times[-1] - q4_end) > 1e-6:
q4_report_times.append(q4_end)
q4_table_radii = [0.0, 0.5, 1.0]
q4_summary = {
"drying_time_s": q4_end,
"drying_time_h": q4_end / 3600.0,
"output_end_s": q4["last_time_s"],
"table_times_h": [time / 3600.0 for time in q4_report_times],
"table_radii_cm": q4_table_radii,
"columns": q4_table_radii + ["药材表面"],
"moisture_table": [
extract_moving_profile(q4, time, q4_table_radii) for time in q4_report_times
],
"radius_at_end_cm": float(radius_history.values(q4_end)[0] * 100.0),
"center_is_max": q4["center_is_max"],
"monotone_profiles": q4["monotone_profiles"],
"maximum_advection_cfl": q4["maximum_advection_cfl"],

28

"picard_max": q4["picard_max"],
"picard_mean": q4["picard_mean"],
}
if run_convergence:
q3_fine = simulate_fixed(
environment,
property_q23,
end_time_s=None,
dt_s=5.0,
output_interval_s=60.0,
stop_at_threshold=True,
)
q4_fine = simulate_moving(
environment,
radius_history,
dt_s=5.0,
output_interval_s=60.0,
stop_at_threshold=True,
)
q4_no_adv = simulate_moving(
environment,
radius_history,
dt_s=10.0,
output_interval_s=60.0,
stop_at_threshold=True,
include_advection=False,
)
q3_summary["time_step_check"] = {
"dt10_s_time_h": q3_end / 3600.0,
"dt5_s_time_h": float(q3_fine["threshold_time_s"]) / 3600.0,
"relative_difference": abs(
float(q3_fine["threshold_time_s"]) - q3_end
) / float(q3_fine["threshold_time_s"]),
}
q4_summary["time_step_check"] = {
"dt10_s_time_h": q4_end / 3600.0,
"dt5_s_time_h": float(q4_fine["threshold_time_s"]) / 3600.0,
"relative_difference": abs(
float(q4_fine["threshold_time_s"]) - q4_end
) / float(q4_fine["threshold_time_s"]),
}
q4_summary["moving_term_comparison"] = {
"with_advection_h": q4_end / 3600.0,
"without_advection_h": float(q4_no_adv["threshold_time_s"]) / 3600.0,
"difference_h": (
float(q4_no_adv["threshold_time_s"]) - q4_end

29

) / 3600.0,
}
write_summary(RESULTS_DIR / "q3" / "summary.json", q3_summary)
write_summary(RESULTS_DIR / "q4" / "summary.json", q4_summary)
print(json.dumps(
{
"q1_surface_temperature_1800_c": q1_summary["temperature_table_c"][-1][-1],
"q1_center_moisture_1800": q1_summary["moisture_table"][-1][0],
"q2_center_temperature_3h_c": q2_summary["temperature_table_c"][-1][0],
"q2_center_moisture_3h": q2_summary["moisture_table"][-1][0],
"q3_drying_time_h": q3_summary["drying_time_h"],
"q4_drying_time_h": q4_summary["drying_time_h"],
"q4_radius_at_end_cm": q4_summary["radius_at_end_cm"],
},
ensure_ascii=False,
indent=2,
))

def main() -> None:
sys.stdout.reconfigure(encoding="utf-8")
parser = argparse.ArgumentParser()
parser.add_argument("--skip-convergence", action="store_true")
args = parser.parse_args()
run_all(run_convergence=not args.skip_convergence)

if __name__ == "__main__":
main()

附录 C

数值质量控制说明

程序在每一步检查温度、水分的有限性与物理范围；问题三、四还检查径向水分
单调性和中心是否为最大值。所有工作簿生成后均重新打开，核对工作表名、行列数、
首末时间、必需数值列及公式错误。结果一至四的尺寸依次为 1801 × 22、10801 × 22、
3373 × 22 和 3121 × 22（尺寸为单表的最大使用区）
。图件同时输出 PDF 矢量版和 PNG
预览版，并检查字体嵌入与边界裁切。

30

