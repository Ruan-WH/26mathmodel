# A 题附件与论文附录要求复审

## 依据与范围

- `A题.pdf` 第 1--3 页：四问结果文件与论文展示表要求；第 3 页附录 1：结果工作簿结构。
- `paper/cumcm-1.1.0/docs/全国大学生数学建模竞赛论文格式规范-2026年修订稿.pdf` 第 1 页第四、五、九、十条，第 2 页第十一条。
- `附件/附件3/result1.xlsx` 至 `result4.xlsx`：官方空白模板，均只有示例起始时间及省略号，不能从模板推断终止时间。
- 本次是交付合规复审：只读检查论文来源、源码引用和工作簿；未重新求解、修改结果文件或重编论文。

## 明确不符合项

1. **附录 B 不完整。** 规范第五条要求建模所用全部完整、可运行源程序；当前 `a2.tex` 仅引入 `solve_drying.py`。至少还须纳入 `write_results.py`、`validate_2d.py`、`run_sensitivity.py`、`run_q4_counterfactual.py`、`make_figures.py`，这些脚本分别支撑结果文件、二维检验、灵敏度、反事实比较和论文图。只保留核心求解程序不能满足该条。
2. **附录 A 清单及复现顺序有缺项。** 清单未列 `run_q4_counterfactual.py`；绘图脚本读取其生成的 `results/q4/fixed_radius_counterfactual.npz`。现有“主求解后运行工作簿、二维、灵敏度和绘图”的说明漏掉这一前置步骤。最终清单应与实际压缩包的相对路径一致，并列出必要中间结果和图表。
3. **支撑材料尚未打包。** A 题目录递归检查未发现 ZIP/RAR 支撑材料文件。规范第九、十一条要求论文与支撑材料分别作为两个电子文件提交，支撑材料压缩为单个 ZIP/RAR，大小不超过 20 MB。项目目录本身不等于已完成交付包。

## 结果文件逐项核对

| 文件 | 题目要求 | 当前检查结果 |
|---|---|---|
| result1.xlsx | 温度、水分浓度两表；时间列单位 s，距离标题单位 cm；1800 s 内每 1 s、每 0.1 cm；四位小数 | 两表各 1801 行含表头、22 列；1--1800 s；0--2 cm；步长和数值精度符合 |
| result2.xlsx | 温度、水分浓度两表；每 1 s、每 0.1 cm；论文展示表为 3 h 内每 0.5 h | 两表各 10801 行含表头、22 列；1--10800 s；步长和精度符合，完整结果的终止范围尚需核定 |
| result3.xlsx | 水分浓度；每 60 s、每 0.1 cm；四位小数 | Sheet1，3373 行含表头、22 列；60--202320 s；0--2 cm；步长与精度符合 |
| result4.xlsx | 水分浓度；每 60 s、每 0.1 cm；末列药材表面；四位小数 | Sheet1，3121 行含表头、22 列；60--187200 s；0--1.9 cm 固定列及药材表面列；超出当时半径处留空 |

工作簿数值区检查未发现 Excel 错误单元格或超过四位小数的存储值，结果数值格式为 `0.0000`。原始模板的距离列省略号已展开，工作表名称保持一致。输出 A1 从模板的“时间\\到药材中心的距离”改为“时间/s”；虽然 A 列与距离标题的语义仍符合题目说明，但提交时宜恢复模板文字或完整注明两轴单位，以避免不必要的模板偏离。

问题二需单独区分：原文先要求建立整个烘干过程模型，再明确论文表格展示 3 h 内的结果，随后要求每秒的“完整结果”。不能把论文表格展示范围自动认定为电子附件的截止范围；官方模板没有给定截止值。本项列为待核定，不把可能的题意解释冒充已确认的硬性违规。

问题三、四的每分钟结果末行分别为 56.2 h、52 h，略晚于论文报告的连续首达时间 56.1863 h、51.9857 h。这与按固定 60 s 网格输出相容，但应在复现说明中区分采样末时刻与连续首达时刻。四位小数显示为 0.1500 不足以判定未四舍五入值是否严格低于阈值，本次不据显示值判失败。

## 其他交付要求

- 正文不超过 30 页，附录不限页数，全部附录应与正文一同提交；不能用多栏缩小源码来替代完整收录。
- 电子论文为单个 PDF 或 Word，不压缩且不超过 20 MB；不含承诺书和编号专用页。
- 支撑材料至少包括全部可运行程序、必要的数据资料及较大中间图表；题目原始数据按规范可不重复提交，若不打包须说明其相对放置位置。
- 论文和支撑材料不能含参赛者、学校、赛区身份信息；本次未进行全部文件的匿名性验收。
- 程序须可运行且结果与论文相符。建议补齐依赖版本、原始数据放置方式及完整运行顺序，并在隔离目录验证支撑包；这属于验证可运行性的措施，而非规范逐字指定的文件名要求。

## 结论

**FAIL。** 附录程序不全、清单遗漏、支撑材料未打包属于已确认问题；问题二时间覆盖范围待核定。原 `VERIFY_REPORT.md` 的全面 PASS 结论撤回。此次未更改论文、结果数值或工作簿。
