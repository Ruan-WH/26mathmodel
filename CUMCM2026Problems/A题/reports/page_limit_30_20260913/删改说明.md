# 附录前压缩至30页的删改说明

日期：2026年9月13日。

本轮按“附录前所有内容恰好30页、尽可能少删除”的要求，压缩重复方法说明、合并重复验证介绍，并取消一处强制分页。本报告以本目录 `before/contents/` 与当前 `paper/cumcm-1.1.0/contents/` 的逐文件差异为依据，记录实际完成的修改，不把未实施的建议列入删改清单。

## 一、页数与保留范围

这里的“附录前”包括摘要、正文、AI工具使用声明和参考文献。页数直接读取修改前后PDF，并以“附录A 支撑材料文件列表”的实际起始页确定，没有采用旧辅助文件的页码。

| 项目 | 修改前 | 修改后 |
|---|---:|---:|
| 附录前所有内容 | 33页 | **30页** |
| 附录A起始页 | 第34页 | **第31页** |
| 附录部分 | 20页 | 20页 |
| 完整PDF | 53页 | **50页** |

修改前PDF为 [paper_before.pdf](paper_before.pdf)，修改后PDF为 [main.pdf](../../paper/cumcm-1.1.0/main.pdf)。

逐文件比较共检查21个 `contents` 文件：7个文件发生文本修改，14个文件未变，没有新增或删除章节源文件。按文本行比较，本轮删除123行、增加19行，净减少104行；这一统计包含空行、LaTeX命令和替换前后的整段文字，不等于删除了123行独立论述。

保留情况已逐项比较：

- 附录前的**14幅图、16张表全部保留**。展开第1至10节实际引用的子文件后，修改前后的14个 `figure` 环境及16个 `table` 环境依次完全相同，包括图表内容、图注表题、标签和插图尺寸命令。
- 14幅实际引用图件的文件哈希与本轮基线相同。正文中的全部数据表内容没有修改。
- 摘要、问题重述、问题分析、六项模型假设、符号表、AI声明、参考文献和附录源文件均未修改。浓度映射、潜热省略、环境延拓、收缩闭合及其限制仍保留。
- 关键控制方程、物性公式、全域首达判据、首达时刻与剖面插值、收缩质量平衡、同物性反事实对照以及验证结果仍保留。
- `baseline_hashes.json` 中35个受保护文件逐一复核，哈希全部一致，覆盖入口文件、类文件、导言区、图件、主求解程序、四份结果工作簿和四问汇总结果等。此次压缩没有重算数值结果。

**确有通用公式展开被删除，不能概括为“所有公式原样保留”。** 本轮删除的3个公式标签为 `eq:pchip`、`eq:q1implicitdetail`、`eq:q2operator`，另压缩了相关无编号的通用或重复展开；具体内容见下文。

## 二、逐项删改及保留位置

以下文件名均相对于论文的 `contents/` 目录。

### 1．`sections/s5.tex`：压缩PCHIP通用公式展开

**删掉或替换的内容：** 删除相邻两点间的局部参数定义、三次Hermite插值的完整线性组合式，以及四个基函数 `h00、h10、h01、h11` 的多项式展开；删除公式标签 `eq:pchip`。原来分别与普通三次样条、分段线性插值作比较的文字，合并为一段方法说明。

**为什么压缩：** 这部分是标准插值方法的展开，占用多行公式空间；本题需要交代的是使用何种插值、如何保持半径形状及如何获得半径导数。

**保留在哪里：** 第5.2节仍明确采用PCHIP，保留节点斜率按相邻割线确定、不在单调区间产生新极值、避免半径非物理回弹、一阶连续和可求取半径导数的说明，并保留 `fritsch1980` 文献。附件数据表、末段统计、环境延拓及其不确定性说明均未变。

**实际删去的细节：** 四个基函数的显式多项式不再在正文列出；它们可在本轮备份和完整对照文档中查到。这一项是删去通用展开，并非该展开仍在论文其他位置逐字保留。

### 2．`sections/s6.tex`：将问题一三段求解说明压缩为三段引用式说明

**删掉或合并的内容：** 删除“径向网格与时间步长的选取”“全隐式控制体方程与边界处理”“逐时推进与迭代终止”三个下级标题；删除重复列出的表面加密网格式、控制体几何、导通系数、三对角方程及Robin系数更新说明；原5个Step改写为连续说明。删除重复公式标签 `eq:q1implicitdetail`。

**为什么压缩：** 第5.5—5.6节已完整给出四问共用的网格、控制体、导通系数、三对角系统、边界处理和Picard流程，问题一无需再次逐式展开。

**保留在哪里：** 当前问题一直接引用 `eq:cv`、`eq:conductance`、`eq:tridiag` 和 `eq:robindiscrete`。本问独有设置仍在第6.2节明确保留：161节点、1秒步长、0—1800秒区间、在新时间层取PCHIP边界值、温度式线性而 `D(C)` 仍迭代、历史状态固定于上一层、双重容差和30次上限、实际最多4次迭代、逐步范围/单调性/通量检查，以及21个物理输出位置。

特征时间、Biot数、结果表、结果图、二维验证和本问末尾结论均未删。

### 3．`sections/s7.tex`：删除无新增信息的抽象算子式

**删掉或替换的内容：** 删除 `H[T;C]=0、M[C;T]=0` 的显示式及标签 `eq:q2operator`；将“两个算子通过系数联系、无需一次组装大矩阵”等说明缩成双向耦合与交替迭代的一句话。

**为什么压缩：** 抽象算子记号没有给出比实际控制方程更多的计算内容。

**保留在哪里：** 第5节保留实际热、水分控制方程及附录3物性；第7.1节保留温度式依赖水分、水分扩散系数依赖温度和水分的耦合关系；第7.2节仍保留温度和水分两条矩阵迭代式。

### 4．`sections/s7.tex`：删除初始物性数值的重复复述

**删掉或替换的内容：** 删除正文中初始密度976.4、比热容3415.3、导热系数0.4830、扩散系数 `5.642×10^-9` 的逐项复述，改为引用代表状态物性表。

**为什么压缩：** 同一组数据已完整列在表 `tab:q2prop` 的“初始全域”一行。

**保留在哪里：** 表格及全部数值未变；正文仍说明两问初始物性不同、即使几何和表面交换相同也会有不同响应，且两问初段解不能混用。

### 5．`sections/s7.tex`：合并变物性求解流程与计算检查

**删掉或合并的内容：** 原“计算网格与非线性系数更新”“温湿场交替求解”合并为“变物性迭代与轨迹衔接”；删除4个重复Step，将边界更新、物性更新、温湿交替求解和收敛后推进的说明并入矩阵式前后。原“步长选取与计算检查”改为“精度与计算检查”，逐项检查清单改引第5节。

**为什么压缩：** 原Step与此前的矩阵式解释、第5.6节统一迭代流程重复。

**保留在哪里：** 两条矩阵迭代式未删；仍保留161节点和1秒步长、重新从初始条件开始、附录3物性、前3小时分析与后续同轨迹衔接、4小时后环境延拓、上一时间层作迭代初值、界面平均及边界处理、先温度后水分、开尔文温度、固定历史状态、实际最多5次迭代、30次上限及 `1.52×10^-5` 步长对照结果。共享轨迹最大差为零的专门检验亦未变。

**额外压缩的说明：** “1秒能够解析60秒采样间隔内的边界变化”和显式格式受最小网格宽度限制的详细比较，不再在本问逐句复述；附件采样间隔、PCHIP处理、1秒步长和仍需时间精度检验的信息继续保留。对删除公式 `eq:q1implicitdetail` 的引用已改为第5节保留的 `eq:tridiag`。

### 6．`sections/s8.tex`：合并首达搜索与插值流程

**删掉或合并的内容：** 将“长期积分与首达区间搜索”和“阈值事件定位方法”合并为“长期积分与阈值事件定位”；删除3个Step、重复的中心控制解释，以及单独成行的阈值跨越条件，改为在首达插值式前用文字给出跨越条件。将“达标剖面与数值精度核验”改为“达标剖面与输出核验”，并合并重复终算设置。

**为什么压缩：** “先检查全域最大值、再确认中心控制”已在第8.1节完整说明；初始化、状态推进、跨越判断又在相邻段落重复出现。

**保留在哪里：** `eq:maxmoisture`、`eq:firstpass`、`eq:monotonecheck`、`eq:centercontrol` 全部保留；首达时刻公式 `eq:eventinterp` 和剖面公式 `eq:profileinterp` 原样保留。仍明确连续积分、不在3小时重置、每步检查单调性、首次出现前层未达标而后层达标时定位、插值权重位于0—1、事件定位与60秒输出分离，以及必要时继续推进到首个后续整分钟的规则。

全部结果、表面提前达标时间、收敛表、二维检验、灵敏度表及情景范围解释均未删。

### 7．`sections/s9.tex` 与 `sections/q4_moving_mesh_validation.tex`：合并两处移动网格验证介绍

**删掉或移入的内容：** 在 `s9.tex` 中删除提前出现的“坐标实现的交叉验证”小标题及介绍段；将“另在物理坐标中建立移动控制体、采用相同物性/环境/步长/首达判据独立计算”的条件，移入 `q4_moving_mesh_validation.tex` 的正式验证段。

**为什么压缩：** 原稿先介绍一次，随后在结果部分再次介绍并列出同一组图表。将说明集中在实际图表附近可减少重复。

**保留在哪里：** “移动网格独立验证”小节、表 `tab:q4movingvalidation`、图 `fig:q4movingvalidation` 和全部误差数值均保留。条件说明扩充到正式验证段；图示精度下重合、浮点量级差异、仅支持实现一致性而不替代实验验证的意思仍完整保留。

### 8．`sections/s9.tex`：删除末尾重复总结

**删掉的内容：** 删除“本问结论与分析”小标题及其一段总结：空间/时间加密相对差均小于 `1.3×10^-5`，结合移动网格与有限元复核支持收缩加快干燥。

**为什么压缩：** 这一段重复紧邻的收敛结果及前文反事实分析，不新增数据或推导。

**保留在哪里：** 第四问收敛段继续给出更具体的空间变化 `5.77×10^-7` 和时间变化 `1.29×10^-5`；移动网格、COMSOL与二维结果均未删；同物性固定半径对照、78.7568小时差值、60.65%时长降幅及物理解释仍在“收缩效应的反事实分解”中。

### 9．`sections/s10.tex`：取消一处强制分页

**删掉的内容：** 仅删除文件开头的 `\newpage`。

**为什么修改：** 允许“模型的评价与推广”接续前文自然排版，利用原有剩余页面空间。

**保留在哪里：** 第10节标题、优点、局限和推广的全部文字均未修改。这一项没有删除实质内容。

## 三、保留图表清单

以下标签在修改前后顺序一致，完整图表环境逐一相同。图表所在页码会随排版前移。

| 图序 | 图标签 |
|---:|---|
| 1 | `fig:roadmap` |
| 2 | `fig:geometry` |
| 3 | `fig:q1` |
| 4 | `fig:q1surface` |
| 5 | `fig:q2` |
| 6 | `fig:q2surface` |
| 7 | `fig:q2diffusivity` |
| 8 | `fig:q3` |
| 9 | `fig:q3front` |
| 10 | `fig:moving` |
| 11 | `fig:q4` |
| 12 | `fig:q4field` |
| 13 | `fig:q4comsol` |
| 14 | `fig:q4movingvalidation` |

| 表序 | 表标签 |
|---:|---|
| 1 | `tab:symbols` |
| 2 | `tab:data` |
| 3 | `tab:q1t` |
| 4 | `tab:q1c` |
| 5 | `tab:q1gap` |
| 6 | `tab:q2prop` |
| 7 | `tab:q2t` |
| 8 | `tab:q2c` |
| 9 | `tab:q3` |
| 10 | `tab:verify` |
| 11 | `tab:sens` |
| 12 | `tab:q4radius` |
| 13 | `tab:q4` |
| 14 | `tab:q4comsolmesh` |
| 15 | `tab:q4movingvalidation` |
| 16 | `tab:q4verify` |

## 四、完整原文与复核入口

- [删改原文与替换对照.md](删改原文与替换对照.md)：覆盖7个修改文件的全部差异组，每组给出完整原文和替换后内容；没有以省略号截断删改原文。
- [changes.diff](changes.diff)：按相对路径生成的完整逐行差异，可核对每一处增删。
- [before/contents](before/contents)：修改前完整章节源文件备份。
- [baseline_hashes.json](baseline_hashes.json)：35个受保护文件的修改前哈希。
- [checks.json](checks.json)：最终页数、受保护文件、附录文字与编译等检查记录。

本报告记录的是本轮页数压缩，不把此前的模型修订归入本轮删改。未修改的图表、假设和数值结论继续沿用原项目的证据与适用范围；本轮排版通过不表示新增了物理模型或实验验证。
