# 附录文字与 Git 工作区清理

截图中的附录 A 表格后两段说明已删除，表格本身及 AI 文件名保留。最终 PDF 仍为 78 页，正文 30 页。与此次修改前逐页比较，只有第 31 页文字变化；正文逐页渲染像素不变。两遍 XeLaTeX 原生退出码均为 0，无缺字、未解析引用或版心溢出。`main.txt`、附录生成脚本和最新检查记录已同步。

## 实际清理

- 移除 `appendix_results_20260913/artifact/node_modules` 临时 junction。它导致 Git 展开列出 7796 项依赖文件；只删除了链接本身，C 盘外部运行环境及其校验文件保持不变。
- 删除两个排版审计的旧预览目录、附录逐页预览及阶段截图。
- 删除阶段编译日志、临时运行时探测脚本、过期进程标识及恢复日志。
- 删除 `recomputed/q1` 至 `q4` 的 9 份重复输出，删除前逐文件确认其 SHA-256 与正式 `results/` 中对应文件相同。
- 删除 4 份已完成用途的一次性排版改写脚本：`group_evidence.py`、`refine_floats.py`、`revise_layout.py`、`compress_repeated_methods.py`。中文删改说明、差异文件及核验脚本仍保留。

除链接外，共实际删除 **164 个文件，约 67.82 MiB**。逐文件路径、大小和删除前哈希见 `cleanup_20260913.json`。

## 保留与防止再次膨胀

保留当前论文、全部模型源码、附录引用源码、图形资产、原始附件、四份 result 工作簿及正式场数据。清理阶段对 113 个论文、代码、输入和结果文件进行了前后哈希检查，均未被清理操作更改。与本次任务有关的 LaTeX 文字修改及重新编译另行核验。

三个历史 `before/` 目录与 `paper_before.pdf` 对应不同阶段，并非相同副本，保留在本地供追溯；它们现在由 `.gitignore` 排除。重新生成的依赖目录、预览、编译中间文件和重复重算全场也加入了忽略规则。规则没有屏蔽正式 PDF、模型代码、原始附件或正式 `results/`。

保留工作簿与独立模型重算的详细结论、源码清单、哈希和比较记录，尤其保留 6 份约 26 KiB 的逐秒首达证据。剩余 Git 变更属于有效论文修改、附录代码和必要核验记录，没有通过回退论文或清空实际成果来减少数量。
