# 当前模型与四问数值结果独立重算核验

核验结论：**PASS（当前模型、保存场与汇总文件的一致性）**。

归档清理说明（2026-09-13）：核验完成后，`recomputed/q1` 至 `recomputed/q4` 中与正式结果逐字节相同的 9 份重复全场和汇总文件已删除，原 `results/` 文件保持不变。本报告、完整比较 JSON、输入及源码哈希、过程记录和 `recomputed/` 顶层的 6 份逐秒首达证据均保留。需要重新计算时可正常运行本目录的 `model_audit_recompute.py`；原恢复模式只适用于当时的中断续算。

导入当前 `code/solve_drying.py`，仅将运行时的 `RESULTS_DIR` 重定向至本目录的 `recomputed/`。保持物性、边界、计算节点、步长、求解器与终止判据不变，重新执行四问主算及现有汇总中涉及的对照计算。没有覆盖原 `code/`、`results/`、`paper/` 或 Excel 文件。

## 逐数组、逐汇总字段比较

| 对象 | 比较数组数 | 比较数组值数 | 最大绝对差 | summary 字段差异数 |
|---|---:|---:|---:|---:|
| Q1 | 4 | 77,464 | 0 | 0 |
| Q2 | 6 | 8,898,526 | 0 | 0 |
| Q3 | 6 | 148,413 | 0 | 0 |
| Q4 | 7 | 994,191 | 0 | 0 |
| 合计 | 23 | 10,118,594 | 0 | 0 |

全部键、形状与数组元素严格相等；包括温度、水分、时间、空间坐标、移动半径及保存的首达温湿剖面。四份 `summary.json` 的所有字段也严格相等，没有采用宽松容差掩盖差异。

另完整复算 Q3/Q4 的 2 s 步长检查、Q4 原固定空间标量结构对照，以及附录4物性下的固定半径反事实。反事实 `fixed_radius_counterfactual.npz` 全部数组严格相等，其首达时长重新得到 129.84453984602226 h；与移动模型的时长差为 78.75684054645612 h，缩短比例为 0.6065471881979088。

## 1 s 首达事件证据

重算包装器在原生 161 个计算节点上记录首次跨阈值的前后两步，不改动原求解器数值。

| 对象 | 阈值前时间 / s | 前一步全域最大 C | 后一步全域最大 C | 插值首达时间 / s |
|---|---:|---:|---:|---:|
| Q3 | 206898 | 0.1500002537314416 | 0.1499999624300792 | 206898.87102730837 |
| Q4 | 183915 | 0.15000036843115117 | 0.14999985492282591 | 183915.71747843814 |

后一步分别为 206899 s 与 183916 s。两问所有此前内部时间步的全域最大水分均大于 0.15；插值剖面的最大水分均等于 0.15，位于中心。对应时长为 Q3 57.47190861869677 h、Q4 51.08769929956615 h。

规律输出继续至首达后的第一个整分钟，分别为 206940 s 与 183960 s；这些输出终点不替代插值得到的首达时间。四位小数展示值不用于判定达标。

## 当前论文与代码对应

已逐项核对：三组物性公式及开尔文转换、初始场、PCHIP 边界重构、14400 s 后的 50℃/0.05 延拓、Robin 边界符号及交换系数、161 节点与 1 s 主算、有限体积与 Picard 迭代、全域首达判据和输出插值规则，未发现实质不一致。

Q4 主算的 `include_advection=False` 只消除相对材料速度项，实际半径仍随附件变化；内部扩散保留 `R^-2`、表面交换保留 `R^-1`。这与论文 `v_s=w`、干骨架守恒及干基含水率的材料坐标推导相符。热方程仍采用明确说明的有效储热模型，没有潜热及完整焓通量。

Q1—Q3 的保存剖面为 21 个规定物理位置，不代表只用了 21 个计算节点；Q4 保存全部 161 个材料节点。Q2 长期保存场用于与 Q3 共享轨迹；按用户最新要求，`result2.xlsx` 的验收范围为 1—10800 s，长期 `q2/fields.npz` 不构成输出范围问题。

以上结论验证当前简化模型的实现和文件一致性，不将其解释为对环境浓度映射、无潜热或均匀收缩假设的实验验证。

## 过程与证据文件

原模型源码、两份输入附件、四问 fields/summary 以及原固定半径反事实共 12 个文件，重算前后 SHA256 全部不变。模型源码 SHA256 为 `93609143157003e52347ca0b988596eafce937c958935414a1b6e85adea210e6`。

用户插话使首个会话在最后一项反事实尚未完成时中断。恢复时复用此前六项已完成计算，仅重跑未完成的反事实；最终七条计算轨迹全部完成。机器报告中的 `elapsed_s` 是恢复会话用时，各项计算用时另见 `simulation_calls`，不应将恢复会话用时解释为全部核验总用时。

- `model_audit_recompute.json`：全部数组、JSON 字段、首达时刻及文件哈希的比较记录。
- `model_audit_consistency.json`：论文—代码十项对应关系及文件行号。
- `model_audit_event_brackets.json`：主算与各对照的原生节点首达记录。
- `recomputed/property_q23_2_event_bracket.npz`、`recomputed/moving_3_event_bracket.npz`：Q3/Q4 阈值前、后及插值时刻的完整 161 节点水分场。
- `model_audit_progress.jsonl`：包含中断前后阶段的运行记录。
- `model_audit_recompute.py`：隔离重算和比较脚本；`--resume` 仅用于本次未完成反事实的恢复。
