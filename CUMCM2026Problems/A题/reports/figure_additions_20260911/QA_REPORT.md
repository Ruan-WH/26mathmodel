# 新增图验收记录

结果：新增 5 张图并写入对应模型章节，全部原图保留。论文原稿已备份为 `paper_before.pdf`。

## 图文位置

|图号|内容|论文页码|
|---|---|---|
|图 3|Q1 预热温度三维曲面|13|
|图 5|Q2 前 3 h 水分三维曲面|17|
|图 6|Q2 扩散系数时空云图及等值线|17|
|图 8|Q3 达标外层与未达标核心|21|
|图 11|Q4 收缩水分场及等比例截面|26|

共 51 页，附录前 31 页（包括摘要、AI 声明和参考文献），符合用户本轮允许超过 30 页的要求。附录从第 32 页开始。

## AP / CL / VI / VV

- AP：已检查生产资产语义；分类计数热图不能直接代替连续物理场，按契约记录参数继承。无随机演示数据、装饰性三维、彩虹色或显著性标记。
- CL：脚本包含字体、配色和导出基线；PDF 与 300 dpi PNG 成对导出。原始画布宽 183 mm，论文按 0.96 倍版心宽度插入，成稿最小标注大于 5 pt。五个 PDF 均验证 SimSun 与 Times New Roman 实际嵌入。
- VI：字段有限、时间递增、数组形状正确、水分非负且径向非增。Q1 所有 1801×21 温度值、Q2 前 3 h 全部 10801×21 水分及温度值均参与计算；未抽稀三维曲面的时间或空间单元。Q3/Q4 纳入全部保存时间直到精确事件，不把事件后的整分钟点当作首达时刻。
- VI：Q3 的区域分界由保存径向值插值，仅作阈值区域说明；正文明确不是相变界面或独立高精度追踪。Q4 圆盘明确是轴对称重建，不冒充二维验证结果；各时刻共享实际长度比例和水分色标，材料域外留白。
- VV：检查五张单图和全文第 13、17、21、26 页，修复三维轴标签裁切、标签距坐标轴过远、等值线亮区文字对比不足。末版第 13、17 页重新渲染复核，图注、单位、刻度、图文衔接可读且未越出版心。色彩之外另有三维高度、阈值红线、区域纹理、边界线与数字标注。
- 保留性：22 个已有图形及源文件（含论文副本）SHA-256 完全不变，详见 `verification.json`。核心求解程序与结果文件未修改。
- LaTeX：latexmk 因环境缺少 Perl 不可用，按已有写作流程改用 XeLaTeX 重复编译；末次无未解析交叉引用、缺字、缺图、过宽/过高盒子。环境仍有既有 LaTeX 发行日期、宋体粗体回退、PDF 书签及用户日志目录提示，不影响成功生成 PDF，未改模板或安装环境。
- PDF 以数据层栅格化控制密集曲面文件体积，文字、色标、坐标和轮廓保留矢量；不将其宣称为所有元素均矢量。

## 统计与来源

本轮是既有确定性 PDE 解的可视化，不新增独立实验或随机重复。n 表示时间×径向输出节点数，不是统计样本量；中心线表示物理轴线而非均值。无 SD、SEM、CI、显著性检验或多重比较。D 由 `solve_drying.property_q23` 对同一 T、C 状态计算。源数据路径、SHA-256、时空尺寸、范围和关键数值保存在 `metrics.json` 和 `verification.json`；Q3 插值前沿另存 `q3_front_data.npz`。

复现（项目根目录）：

```powershell
& D:/Anaconda/python.exe CUMCM2026Problems/A题/code/make_additional_figures.py
```

在 `paper/cumcm-1.1.0` 目录连续运行两次 `xelatex -interaction=batchmode -halt-on-error main.tex`，然后在项目根运行：

```powershell
& D:/Anaconda/python.exe CUMCM2026Problems/A题/reports/figure_additions_20260911/verify_artifacts.py
```

复核由代码检查与 AI 视觉检查完成；最终审阅由用户进行，本记录不冒充人工审查。
